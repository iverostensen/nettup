---
phase: 26-seo-og-intern-lenking
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified: []
autonomous: false
requirements: [LINK-03]

must_haves:
  truths:
    - "All /steder/* city page URLs are present in the deployed sitemap"
    - "V2 promotion criteria are acknowledged — no V2 work begins before V1 indexing is confirmed"
  artifacts:
    - path: ".planning/phases/26-seo-og-intern-lenking/26-02-SUMMARY.md"
      provides: "Sitemap verification result (pass/fail + URL count)"
  key_links:
    - from: "https://nettup.no/sitemap-index.xml"
      to: "/steder/* pages"
      via: "sitemap entry"
      pattern: "/steder/"
---

<objective>
Verify that all city pages appear in the deployed sitemap, satisfying LINK-03 and formally closing the v1 milestone.

Purpose: LINK-03 is a post-deploy gate — it cannot be satisfied by code alone. This plan records the manual verification result so the requirement has an auditable closure.
Output: Confirmed sitemap coverage documented in SUMMARY.md.
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/26-seo-og-intern-lenking/26-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify city pages appear in deployed sitemap</name>
  <what-built>
    Phase 26 code changes are complete (plan 26-01):
    - BaseLayout.astro now has a dynamic areaServed array auto-derived from locations.ts
    - astro.config.mjs assigns priority 0.8 to all /steder/* sitemap entries
    - locations.ts documents V2 promotion criteria above ACTIVE_TIER

    The site must now be deployed to production (git push → Vercel auto-deploy) before sitemap verification is possible.
  </what-built>
  <how-to-verify>
    1. Deploy: push the phase-26 branch/changes to main and let Vercel build complete
    2. Wait ~1–2 minutes for deployment to go live
    3. Fetch the sitemap index:
       ```
       curl https://nettup.no/sitemap-index.xml
       ```
       Note the sub-sitemap URL(s) listed (e.g. `https://nettup.no/sitemap-0.xml`)
    4. Fetch the sub-sitemap and count /steder/ entries:
       ```
       curl https://nettup.no/sitemap-0.xml | grep -c "/steder/"
       ```
       Expected: 8 (one per Tier 1 city: oslo, drammen, asker, baerum, lillestrom, sandvika, ski, moss)
    5. Confirm the priority for a city URL:
       ```
       curl https://nettup.no/sitemap-0.xml | grep -A3 "/steder/oslo"
       ```
       Expected: `<priority>0.8</priority>`
  </how-to-verify>
  <resume-signal>
    Type "verified: N cities in sitemap" where N is the count from step 4, or describe any issues found.
  </resume-signal>
</task>

</tasks>

<verification>
LINK-03 is satisfied when:
- At least 8 /steder/* URLs appear in the deployed sitemap
- Each has priority 0.8

LINK-04 is already satisfied by the JSDoc in locations.ts (written in plan 26-01) — no action needed here.
</verification>

<success_criteria>
- Human confirms all 8 city pages are present in the live sitemap
- SUMMARY.md records the verification result (URL count + priority confirmed)
- v1.5 milestone is complete — all 12 requirements satisfied
</success_criteria>

<output>
After verification is confirmed, create `.planning/phases/26-seo-og-intern-lenking/26-02-SUMMARY.md` recording:
- Date of verification
- Count of /steder/* URLs found in sitemap
- Priority value confirmed
- LINK-03: SATISFIED
- v1.5 milestone status: COMPLETE
</output>
