---
phase: 25-tier-1-innhold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/locations.ts
autonomous: true
requirements:
  - CONTENT-01
  - CONTENT-02
  - SEO-02

must_haves:
  truths:
    - "All 8 Tier 1 cities have entries in locations.ts with genuinely differentiated intro copy"
    - "Every city entry has 2–3 city-specific FAQ items covering local presence, process, and pricing"
    - "Every city entry has a unique metaTitle and metaDescription"
    - "nearbyAreas slugs are symmetric across all 8 entries"
    - "npm run build passes cleanly with all 8 city pages generated"
  artifacts:
    - path: "src/config/locations.ts"
      provides: "8 complete Tier 1 city entries"
      contains: "oslo, baerum, asker, sandvika, drammen, lillestrom, ski, moss"
  key_links:
    - from: "nearbyAreas in each city entry"
      to: "slug fields of other city entries"
      via: "ASCII slug match"
      pattern: "nearbyAreas.*slug"
---

<objective>
Write all 8 Tier 1 city entries in `src/config/locations.ts` — replacing the two Oslo/Bærum stubs with full content and adding 6 new cities (Asker, Sandvika, Drammen, Lillestrøm, Ski, Moss).

Purpose: This is the primary content task for Phase 25. All downstream tasks (JSON-LD, footer, kontakt) depend on these entries existing.
Output: `locations.ts` with 8 complete, unique city entries ready for static generation.
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-tier-1-innhold/25-CONTEXT.md
@.planning/phases/25-tier-1-innhold/25-RESEARCH.md

<interfaces>
<!-- City interface from src/config/locations.ts -->
```typescript
export const ACTIVE_TIER = 1;

export interface City {
  tier: 1 | 2 | 3;
  slug: string;          // ASCII only — æ→ae, ø→o, å→a, spaces→hyphen
  name: string;          // Norwegian display name
  intro: string;         // 2–3 sentences, locally specific neighborhood references
  faq: Array<{ question: string; answer: string }>;  // 2–3 items per city
  nearbyAreas: string[]; // ASCII slugs of nearby cities (must be symmetric)
  metaTitle: string;     // Format: "Nettside for bedrift i {By} | Nettup" (~40 chars)
  metaDescription: string; // 140–160 chars, mentions Nettup + city + value prop
  industries?: string[];
}

export const cities: City[] = [...];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write all 8 city entries in locations.ts</name>
  <files>src/config/locations.ts</files>
  <action>
Replace the existing two stub entries (Oslo, Bærum) and add 6 new entries (Asker, Sandvika, Drammen, Lillestrøm, Ski, Moss). Keep ACTIVE_TIER = 1, the slug convention comment, and the City interface unchanged.

**City order in array:** Oslo, Bærum, Asker, Sandvika, Drammen, Lillestrøm, Ski, Moss

**Slug map (ASCII):**
- Oslo → `oslo`
- Bærum → `baerum`
- Asker → `asker`
- Sandvika → `sandvika`
- Drammen → `drammen`
- Lillestrøm → `lillestrom`
- Ski → `ski`
- Moss → `moss`

**nearbyAreas symmetry map (copy exactly — this has been pre-verified):**
- `oslo`: `['baerum', 'lillestrom']`
- `baerum`: `['oslo', 'asker', 'sandvika']`
- `asker`: `['baerum', 'sandvika', 'drammen']`
- `sandvika`: `['baerum', 'asker', 'oslo']`
- `drammen`: `['asker']`
- `lillestrom`: `['oslo']`
- `ski`: `['moss']`
- `moss`: `['ski']`

**Content requirements:**

Each intro MUST reference specific local neighborhoods or landmarks — not generic text with city-name swapped. 2–3 sentences. Tone: professional but approachable (Nettup standard).

- **Oslo**: Mention Grünerløkka, Majorstuen, Aker Brygge
- **Bærum**: Mention Sandvika, Bekkestua, Lysaker (and optionally Fornebu)
- **Asker**: Mention Asker sentrum, Heggedal or Dikemark, proximity to vestkorridoren
- **Sandvika**: Focus on Sandvika as commercial center — Sandvika Storsenter, E18 proximity, business district character. Note it's part of Bærum municipality, but differentiate from Bærum entry which focuses on the broader municipality.
- **Drammen**: Mention Bragernes, Strømsø
- **Lillestrøm**: Mention Lillestrøm sentrum, Romerike-regionen, Ahus-area or Kjeller
- **Ski**: Mention Ski sentrum, Follo-regionen
- **Moss**: Mention Moss sentrum, Jeløya or Refsnes, Østfold connection

**FAQ format (2–3 per city, covering these three topics in order):**
1. Lokal tilstedeværelse: "Holder dere til i X?" — answer says we work digitally and help businesses across the region, meetings by video.
2. Prosess/samarbeid: "Hvordan samarbeider dere med bedrifter i X?" — answer describes the process: quick call → 24h offer → digital collaboration.
3. Pris/leveringstid: "Hva koster en nettside for bedrifter i X?" — answer MUST mention startpris 15 000 kr and include a plain-text reference to priskalkulator: "Bruk vår priskalkulator på nettup.no/priskalkulator for et raskt estimat." (plain text, no HTML anchor — the template renders this as text, and plain text is also correct for FAQPage JSON-LD in the next plan).

**metaTitle pattern:** `"Nettside for bedrift i {By} | Nettup"` — keep this exact format for all 8 cities.

**metaDescription pattern:** 140–160 characters. Include: "Nettup lager profesjonelle nettsider for bedrifter i {By}." + key value prop + call to action. Vary the value prop wording across cities (don't use identical text).

After writing all entries, do a self-check: for each city, verify that every slug listed in its `nearbyAreas` exists as a `slug` field in another city entry. This prevents broken pills.
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build passes. 8 city pages generated at /steder/{slug} for all 8 cities. Each entry has tier:1, unique intro with local neighborhoods, 2–3 FAQ items, nearbyAreas slugs matching the symmetry map, metaTitle, metaDescription.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. Build output shows 8 pages under /steder/ (oslo, baerum, asker, sandvika, drammen, lillestrom, ski, moss)
3. nearbyAreas symmetry: every slug in any nearbyAreas array corresponds to an existing city entry slug
4. No city has a boilerplate intro — each mentions specific local neighborhoods
5. All FAQ price answers mention "15 000 kr" and "priskalkulator"
</verification>

<success_criteria>
8 unique, complete Tier 1 city entries in locations.ts. Build passes. All 8 /steder/{slug} pages rendered successfully.
</success_criteria>

<output>
After completion, create `.planning/phases/25-tier-1-innhold/25-01-SUMMARY.md`
</output>
