---
phase: 30-traceability-og-nav-verifikasjon
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/28-floatingnav-rewrite/28-VERIFICATION.md
autonomous: false
requirements: [ANAL-01, ANAL-02, ANAL-03, NAV-01, NAV-02, NAV-03]

must_haves:
  truths:
    - "All 19 v1 requirements in REQUIREMENTS.md show status Complete"
    - "NAV-01/02/03 checkboxes in the v1 Requirements list are checked (- [x])"
    - "NAV-01/02/03 traceability rows show Phase 28 (no / Phase 30 suffix)"
    - "ANAL-01/02/03 traceability rows show Phase 27 (no / Phase 30 suffix)"
    - "28-VERIFICATION.md frontmatter status is `passed` and score is `7/7`"
    - "28-VERIFICATION.md body contains a human sign-off section confirming all 4 tests"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Authoritative v1 requirement traceability"
      contains: "| NAV-01 | Phase 28 | Complete |"
    - path: ".planning/phases/28-floatingnav-rewrite/28-VERIFICATION.md"
      provides: "Phase 28 verification record"
      contains: "status: passed"
  key_links:
    - from: "checkpoint:human-verify (Task 1)"
      to: "document updates (Task 2)"
      via: "All 4 Phase 28 runtime tests must pass before any edits are made"
      pattern: "wave 1 gate → wave 2 auto"
---

<objective>
Confirm Phase 28 FloatingNav runtime behaviors through browser testing, then update REQUIREMENTS.md traceability and 28-VERIFICATION.md to reflect verified status. No new features — documentation closure only.

Purpose: Close the last open traceability gap in v1.5. Once complete, all 19 v1 requirements show Complete and Phase 28 is fully signed off.
Output: REQUIREMENTS.md with 6 rows cleaned up; 28-VERIFICATION.md upgraded from `human_needed` to `passed`.
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-floatingnav-rewrite/28-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Phase 28 runtime verification — 4 browser tests</name>
  <what-built>
Phase 28 converted FloatingNav from a client:only React island to a server-rendered Astro component with transition:persist. All structural preconditions are confirmed (Phase 28 scored 6/7 automatically). The remaining 4 runtime behaviors require live browser observation.

All 4 tests use the local dev server. You manage the dev server yourself (`npm run dev` at localhost:4321).

If any test fails: apply a targeted fix (see fix guide below), run `npm run build` to confirm it passes cleanly, then re-run the failing test before continuing to Task 2. Do not proceed to Task 2 until all 4 tests pass.
  </what-built>
  <how-to-verify>
**Test 1 — Zero-flash SPA navigation**
Navigate between multiple pages using nav links (e.g. / → /om-oss → /prosjekter → /tjenester/nettside → /).
Expected: Nav pill is visible and correctly styled at all times. No blank period, no flicker, no empty astro-island shell.

**Test 2 — Scroll hide/show**
On any long page, scroll down past 80px, then scroll back up.
Expected: Nav slides up and disappears on scroll down; slides back into view on scroll up. Stays visible when near the top (< 80px).

**Test 3 — Entrance animation once per session**
Open DevTools → Application → Session Storage → clear all. Hard-refresh (Cmd+Shift+R). Then navigate via SPA.
Expected: Nav slides down from top on first load. After SPA navigation, nav does NOT animate again — it stays in place.

**Test 4 — Mobile menu custom event**
Resize to mobile viewport (or DevTools device toolbar). Tap the hamburger icon.
Expected: Full-screen mobile menu opens with Framer Motion animation. Tapping a nav link or close button closes it.

---

**Fix guide (only if a test fails):**

- Test 1 or 7 (flash): Check `transition:persist` attribute in `FloatingNav.astro` line 35; verify `data-astro-transition-persist` id is consistent in built HTML; check `BaseLayout.astro` imports.
- Test 2 (scroll): Check `initScrollBehavior()` call in `FloatingNav.astro` inline script; verify `.nav-hidden` class toggles `translateY(-120%)` in `global.css`.
- Test 3 (animation): Check `sessionStorage.getItem('nav-animated')` gate; verify `animationend` listener removes `.nav-entrance` class.
- Test 4 (mobile menu): Check `dispatchEvent(new CustomEvent('open-mobile-menu'))` in hamburger handler; check `MobileMenu.tsx` `useEffect` listener registration.

After any fix: run `npm run build` (must pass 0 errors), then re-run only the failing test.
  </how-to-verify>
  <resume-signal>Type "all 4 pass" if all tests pass. If a fix was needed, describe what you fixed, then type "all 4 pass" once re-confirmed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md and 28-VERIFICATION.md</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/28-floatingnav-rewrite/28-VERIFICATION.md</files>
  <action>
Make the following targeted edits. No other changes.

**REQUIREMENTS.md — edit 1: NAV checkboxes (lines 41–43)**

Change three unchecked boxes to checked:
```
- [x] **NAV-01**: FloatingNav is server-rendered from first byte — no React hydration gap, no empty `astro-island` shell on page load
- [x] **NAV-02**: FloatingNav persists as a live DOM node across SPA navigations via `transition:persist` — active link reflects current page after every navigation
- [x] **NAV-03**: No `visibility:hidden` body hack (`html[data-loading]` rule) masking the hydration flash — root cause eliminated
```

**REQUIREMENTS.md — edit 2: ANAL traceability rows (lines 95–97)**

Strip "/ Phase 30" from Phase column, leave Status as Complete:
```
| ANAL-01 | Phase 27 | Complete |
| ANAL-02 | Phase 27 | Complete |
| ANAL-03 | Phase 27 | Complete |
```

**REQUIREMENTS.md — edit 3: NAV traceability rows (lines 98–100)**

Strip "/ Phase 30" from Phase column AND flip Status from Pending to Complete:
```
| NAV-01 | Phase 28 | Complete |
| NAV-02 | Phase 28 | Complete |
| NAV-03 | Phase 28 | Complete |
```

**28-VERIFICATION.md — edit 1: Frontmatter**

Update status and score fields, add human_verified date:
```yaml
status: passed
score: 7/7 must-haves verified
human_verified: 2026-03-13
```

**28-VERIFICATION.md — edit 2: Status line in body (line 25)**

Change `**Status:** human_needed` to `**Status:** passed`

**28-VERIFICATION.md — edit 3: Anti-Patterns section**

The info-level row for `FloatingNav.tsx` dead code is now resolved. After the existing row, add a note:
```
> **Resolved (Phase 29, commit bbb982e):** `FloatingNav.tsx` was deleted in Phase 29 gap closure. Dead code no longer present.
```

**28-VERIFICATION.md — edit 4: Add Human Sign-off section after "Gaps Summary"**

Append before the final `_Verified:` line:

```markdown
---

### Human Verification Sign-off

**Verified:** 2026-03-13
**All 4 runtime tests passed:**

| # | Test | Result |
|---|------|--------|
| 1 | Zero-flash SPA navigation | PASS |
| 2 | Scroll hide/show behavior | PASS |
| 3 | Entrance animation — once per session | PASS |
| 4 | Mobile menu — custom event trigger | PASS |

All 7 observable truths confirmed. Phase 28 status upgraded from `human_needed` to `passed`.
```

After edits, verify no other rows were accidentally modified and the total count (19 requirements) is unchanged.
  </action>
  <verify>
    <automated>grep -n "NAV-01\|NAV-02\|NAV-03\|ANAL-01\|ANAL-02\|ANAL-03" /Users/iverostensen/nettup/.planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
- REQUIREMENTS.md: 3 NAV checkboxes are `[x]`, ANAL rows show `Phase 27` only, NAV rows show `Phase 28 | Complete`
- 28-VERIFICATION.md: frontmatter `status: passed`, `score: 7/7`, `human_verified: 2026-03-13`; body has human sign-off table; anti-patterns note references Phase 29 cleanup
  </done>
</task>

</tasks>

<verification>
After Task 2 completes:

```bash
grep -c "\- \[x\]" /Users/iverostensen/nettup/.planning/REQUIREMENTS.md
# Should return 19 (all v1 requirements checked)

grep "status:" /Users/iverostensen/nettup/.planning/phases/28-floatingnav-rewrite/28-VERIFICATION.md
# Should return: status: passed

grep "Phase 30" /Users/iverostensen/nettup/.planning/REQUIREMENTS.md
# Should return nothing (all / Phase 30 references stripped)
```
</verification>

<success_criteria>
- All 19 v1 requirements in REQUIREMENTS.md show Complete with no Pending entries
- NAV-01/02/03 checkboxes are `[x]` in the requirements list
- NAV-01/02/03 traceability rows: `Phase 28 | Complete`
- ANAL-01/02/03 traceability rows: `Phase 27 | Complete`
- 28-VERIFICATION.md frontmatter: `status: passed`, `score: 7/7 must-haves verified`
- 28-VERIFICATION.md body contains human sign-off section with 4-test pass table
- Phase 28 FloatingNav runtime behavior confirmed by human observation
</success_criteria>

<output>
After completion, create `.planning/phases/30-traceability-og-nav-verifikasjon/30-01-SUMMARY.md`
</output>
