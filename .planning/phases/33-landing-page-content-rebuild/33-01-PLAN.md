---
phase: 33-landing-page-content-rebuild
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/nettside-for-bedrift/_sections/Hero.astro
  - src/pages/nettside-for-bedrift/_sections/VisualProof.astro
  - src/components/islands/HeroMicroForm.tsx
autonomous: true
requirements: [LP-01, LP-04, LP-06]

must_haves:
  truths:
    - "Hero shows price anchoring: competitor price crossed out, then 0 kr oppstart + 399 kr/mnd"
    - "4.9-star rating block is completely removed from Hero"
    - "Trust badges show '30 dagers garanti' and 'Ingen bindingstid' (not '24t responstid')"
    - "Scroll indicator says 'Se hva som er inkludert' (not 'Se vare pakker')"
    - "Micro-testimonial from iGive appears below the hero form"
    - "VisualProof shows website preview without Lighthouse score metrics overlay"
    - "HeroMicroForm button text is subscription-aligned (not 'Fa gratis tilbud')"
  artifacts:
    - path: "src/pages/nettside-for-bedrift/_sections/Hero.astro"
      provides: "Price-anchored hero with micro-testimonial"
      contains: "subscriptionOffer"
    - path: "src/pages/nettside-for-bedrift/_sections/VisualProof.astro"
      provides: "Visual website preview section"
      contains: "iGiveImage"
    - path: "src/components/islands/HeroMicroForm.tsx"
      provides: "Updated micro-form button text"
  key_links:
    - from: "Hero.astro"
      to: "subscriptionOffer.ts"
      via: "import"
      pattern: "subscriptionOffer\\.price"
---

<objective>
Rebuild the above-fold experience: Hero with price anchoring against one-time costs, subscription pricing from subscriptionOffer.ts, updated trust badges, micro-testimonial, and VisualProof with website previews instead of Lighthouse scores.

Purpose: A Google Ads visitor landing on this page must immediately see one clear subscription offer with honest social proof, not old tier pricing or fake star ratings.
Output: Hero.astro, VisualProof.astro, HeroMicroForm.tsx updated
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/config/subscriptionOffer.ts

<interfaces>
<!-- subscriptionOffer.ts shape (single source of truth for all pricing) -->
```typescript
export interface SubscriptionOffer {
  price: { setupPrice: number; monthlyPrice: number; };
  features: string[];
  terms: { binding: string; cancellation: string; };
  upsellLinks: Array<{ label: string; href: string; }>;
  meta: { title: string; description: string; };
}
// Values: setupPrice=0, monthlyPrice=399, binding="Ingen bindingstid"
```

<!-- Testimonial.astro quote (use shortened version for micro-testimonial) -->
```
Full quote: "Vi trengte en nettside som lastet raskt og sa profesjonell ut. Nettup leverte pa under to uker -- og resultatet snakker for seg selv. 95 i ytelse og under ett sekund lastetid. Endelig en leverandor som faktisk forstar hva som fungerer."
Author: Stein Eriksen, Daglig leder, iGive
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Hero.astro with price anchoring, updated badges, and micro-testimonial</name>
  <files>src/pages/nettside-for-bedrift/_sections/Hero.astro</files>
  <read_first>
    - src/pages/nettside-for-bedrift/_sections/Hero.astro (current state)
    - src/config/subscriptionOffer.ts (pricing values)
    - src/pages/nettside-for-bedrift/_sections/Testimonial.astro (iGive quote text for micro-testimonial)
  </read_first>
  <action>
Rewrite Hero.astro with these specific changes:

1. **Add subscriptionOffer import** at top of frontmatter:
   ```
   import { subscriptionOffer } from '@/config/subscriptionOffer';
   ```

2. **Replace value proposition paragraph** (lines 45-50, the "7 000 kr / Fra 2 500 kr / Lanseringstilbud" block). New content:
   - Price anchoring line: "Andre tar 15 000+ kr" (crossed out or muted), then "Hos oss: 0 kr oppstart" using `subscriptionOffer.price.setupPrice`, then "kun {subscriptionOffer.price.monthlyPrice} kr/mnd"
   - Remove the "Lanseringstilbud" badge span
   - Keep "Klar pa 1-3 uker" delivery timeline text

3. **Replace trust badges** (lines 53-89):
   - Badge 1: Keep "30 dagers garanti" with shield icon (already exists, keep as-is)
   - Badge 2: Change "24t responstid" to "Ingen bindingstid" -- replace the clock icon with an unlock/no-binding icon (use Heroicons `LockOpen` path: `M13.5 10.5V6.75a4.5 4.5 0 1 1 9 0v3.75M3.75 21.75h10.5a2.25 2.25 0 0 0 2.25-2.25v-6.75a2.25 2.25 0 0 0-2.25-2.25H3.75a2.25 2.25 0 0 0-2.25 2.25v6.75a2.25 2.25 0 0 0 2.25 2.25Z`)

4. **Remove the entire star rating block** (lines 97-119, the div with "4.9 / 5 basert pa kundeanmeldelser" and five yellow star SVGs). Delete completely.

5. **Add micro-testimonial below the form** (after the HeroMicroForm div, before the secondary CTA). New markup:
   ```html
   <div class="reveal-on-scroll delay-4 mt-6 flex items-center gap-3 justify-center lg:justify-start">
     <div class="flex h-10 w-10 shrink-0 items-center justify-center rounded-lg bg-white p-1.5">
       <img src="/images/igive-logo.svg" alt="iGive logo" class="h-full w-full object-contain" />
     </div>
     <p class="text-sm text-text-muted italic">
       "Nettup leverte pa under to uker -- resultatet snakker for seg selv."
       <span class="not-italic text-text-muted/70 block mt-0.5">Stein Eriksen, iGive</span>
     </p>
   </div>
   ```

6. **Update scroll indicator text** (line 141): Change "Se vare pakker" to "Se hva som er inkludert"

7. **Update secondary CTA delay class** from `delay-5` to `delay-5` (keep same, but renumber delays if needed since star rating removal shifts things). The delay sequence should be: h1 (none), value prop (delay-1), badges (delay-2), form (delay-3), micro-testimonial (delay-4), secondary CTA (delay-5).

Keep: LandingHeroAnimation import and rendering, HeroMicroForm import and rendering, iGiveImage import, background gradient, grid layout.
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Hero.astro contains `import { subscriptionOffer } from '@/config/subscriptionOffer'`
    - Hero.astro contains `15 000` (anchoring price)
    - Hero.astro contains `subscriptionOffer.price.setupPrice` or `0 kr` from config
    - Hero.astro contains `subscriptionOffer.price.monthlyPrice` or `399` from config
    - Hero.astro contains `Ingen bindingstid` in trust badge text
    - Hero.astro does NOT contain `4.9` or `kundeanmeldelser` or `fill-yellow-400`
    - Hero.astro does NOT contain `24t responstid`
    - Hero.astro does NOT contain `Fra 2 500 kr` or `7 000 kr` or `Lanseringstilbud`
    - Hero.astro contains `Stein Eriksen` (micro-testimonial)
    - Hero.astro contains `Se hva som er inkludert` (scroll indicator)
    - Build succeeds with no errors
  </acceptance_criteria>
  <done>Hero shows price anchoring against 15 000+ kr, subscription pricing from config, "Ingen bindingstid" badge, micro-testimonial from iGive, no star rating, scroll indicator says "Se hva som er inkludert"</done>
</task>

<task type="auto">
  <name>Task 2: Replace VisualProof Lighthouse metrics with website preview and update HeroMicroForm button</name>
  <files>src/pages/nettside-for-bedrift/_sections/VisualProof.astro, src/components/islands/HeroMicroForm.tsx</files>
  <read_first>
    - src/pages/nettside-for-bedrift/_sections/VisualProof.astro (current Lighthouse metrics)
    - src/components/islands/HeroMicroForm.tsx (current button text)
  </read_first>
  <action>
**VisualProof.astro changes:**

1. **Update section header:**
   - h2: Change "Resultater som teller" to "Slik kan din nettside se ut" (more relatable for non-technical SMB)
   - p: Change description to "Vi bygger moderne, raske nettsider som fungerer pa alle enheter. Her er et eksempel fra en av vare kunder."

2. **Remove the metrics overlay grid** (the 3-column grid with Ytelse 95, Lastetid <1s, SEO 100). Delete the entire `grid grid-cols-3 gap-4 mb-6` div.

3. **Keep the screenshot** with iGive Image, `<Image>` component, and the iGive logo badge overlay. These stay exactly as-is.

4. **Update attribution text**: Change "Faktisk prosjekt levert i januar 2026." to "Levert for iGive i januar 2026."

5. **Update trust reinforcement**: Change "Alle vare nettsider leveres med samme kvalitetsstandarder" to "Alle nettsider inkluderer responsivt design, SEO og SSL-sertifikat"

Keep: Image import, astro:assets import, logo badge overlay, reveal-on-scroll classes, lazy loading.

**HeroMicroForm.tsx changes:**

1. **Update button text** (line 145): Change `'Fa gratis tilbud'` to `'Kom i gang'` (matches subscription CTA language used in PricingSummary)

2. **Update helper text** (line 170): Change "Vi svarer innen 24 timer. Ingen forpliktelser." to "0 kr oppstart. Ingen bindingstid."
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - VisualProof.astro contains "Slik kan din nettside se ut"
    - VisualProof.astro does NOT contain "grid-cols-3" (metrics grid removed)
    - VisualProof.astro does NOT contain "Ytelse" or "Lastetid" or "Lighthouse"
    - VisualProof.astro contains `iGiveImage` import (screenshot kept)
    - HeroMicroForm.tsx contains `'Kom i gang'` as button text
    - HeroMicroForm.tsx contains `0 kr oppstart` in helper text
    - HeroMicroForm.tsx does NOT contain `Fa gratis tilbud`
    - Build succeeds
  </acceptance_criteria>
  <done>VisualProof shows website preview without Lighthouse metrics, HeroMicroForm has subscription-aligned button text and helper text</done>
</task>

</tasks>

<verification>
- `npm run build` passes
- No references to old pricing (2 500, 7 000, Lanseringstilbud) in Hero.astro
- No star rating markup in Hero.astro
- No Lighthouse metrics in VisualProof.astro
- subscriptionOffer.ts imported in Hero.astro
</verification>

<success_criteria>
Above-fold experience presents one subscription offer with price anchoring, honest trust signals, and website preview instead of technical metrics.
</success_criteria>

<output>
After completion, create `.planning/phases/33-landing-page-content-rebuild/33-01-SUMMARY.md`
</output>
