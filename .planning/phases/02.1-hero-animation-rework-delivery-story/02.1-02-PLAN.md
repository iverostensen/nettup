---
phase: 02.1-hero-animation-rework-delivery-story
plan: 02
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "The delivery story animation is visually correct and tells the intended narrative"
    - "The animation loops without flicker or layout shift"
    - "Reduced motion fallback is verified to show the static done state"
  artifacts:
    - path: "src/components/islands/HeroDeliveryAnimation.tsx"
      provides: "Animation that has passed human visual review"
  key_links:
    - from: "localhost:4321"
      to: "HeroDeliveryAnimation component"
      via: "browser dev tools / visual inspection"
      pattern: "human observation"
---

<objective>
Human visual verification of the HeroDeliveryAnimation. Claude runs the dev server; the user confirms the animation tells the delivery story correctly before the phase is marked complete.

Purpose: Animation quality cannot be fully verified by automated tests. This checkpoint ensures the narrative is compelling, the timing feels right, and the loop is seamless before shipping.

Output: User approval (or list of issues for a gap-closure plan).
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-hero-animation-rework-delivery-story/02.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server for visual verification</name>
  <files></files>
  <action>
Start the Astro dev server and confirm it is running:

```bash
cd /Users/iverostensen/nettup && npm run dev
```

Confirm the server starts at `http://localhost:4321`. The homepage (/) is where the animation is visible — specifically the right column of the hero section (desktop viewport, lg: breakpoint, ≥1024px wide).
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && curl -s -o /dev/null -w "%{http_code}" http://localhost:4321/ 2>/dev/null || echo "Server not running — start with: npm run dev"</automated>
  </verify>
  <done>Dev server running at localhost:4321</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification of delivery story animation</name>
  <action>Present the verification checklist to the user and wait for approval or issue report.</action>
  <verify>User types "approved"</verify>
  <done>User has confirmed the animation is visually correct and loops cleanly</done>
  <what-built>
HeroDeliveryAnimation — a looping cinematic animation in the right column of the hero section that tells Nettup's "2-week delivery" story in ~4 seconds:

Phase 1 (~1.8s): A day counter ticks from 1 to 14 in brand cyan, with "/ 14 dager" label below it.
Phase 2 (~0.5s): A browser mockup appears. Four abstract rectangles (nav, hero, cards, footer) snap in with a stagger from top to bottom.
Phase 3 (~0.3s): A "✓ Lansert!" badge pops in with a spring scale animation.
Rest (2.5s): Everything stays visible — the "done" state.
Loop reset: Everything snaps back to initial state (instant), day counter resets to 1, sequence repeats.
  </what-built>
  <how-to-verify>
1. Open `http://localhost:4321` in a browser at ≥1024px viewport width (the right column is desktop-only).
2. Watch the right column of the hero for at least 2 full loops (~8–10 seconds).

Verify each phase in order:
- [ ] Day counter starts at 1 and ticks steadily up to 14 (should feel brisk — ~1.8s total)
- [ ] At 14, the browser mockup appears: 4 rectangles snap in from top to bottom with a stagger
- [ ] "✓ Lansert!" badge pops in with a spring (slight overshoot then settle)
- [ ] Everything stays visible for ~2.5 seconds
- [ ] Reset is instant (no fade-out animation) — day jumps back to 1, sections disappear
- [ ] Second loop starts cleanly from day 1

Also verify:
- [ ] Left column (heading, RotatingText, description, CTAs) is completely unaffected
- [ ] The animation card has the same dark card visual treatment as the old stats card
- [ ] No console errors in browser DevTools during the loop

Optional (if you want to test reduced motion):
- In Chrome DevTools → Rendering → Emulate CSS media feature → prefers-reduced-motion: reduce
- Reload the page — the right column should show a static card with day 14, all sections visible, "✓ Lansert!" badge — no animation
  </how-to-verify>
  <resume-signal>
Type "approved" to mark the phase complete, or describe specific issues (e.g., "day counter too fast", "badge doesn't appear", "loop has a flash") so they can be fixed in a gap-closure plan.
  </resume-signal>
</task>

</tasks>

<verification>
Human approval is the verification. No automated checks beyond the dev server being reachable.
</verification>

<success_criteria>
User types "approved" — confirming the delivery story animation is visually correct, loops cleanly, and tells the intended narrative.
</success_criteria>

<output>
After approval, create `.planning/phases/02.1-hero-animation-rework-delivery-story/02.1-02-SUMMARY.md`

Include:
- Verification result (approved / issues found)
- Any visual notes the user provided
- Phase 02.1 marked complete if approved
</output>
