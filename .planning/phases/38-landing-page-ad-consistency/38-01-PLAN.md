---
phase: 38-landing-page-ad-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/nettside-for-bedrift/_sections/PricingSummary.astro
  - src/layouts/LandingPageLayout.astro
autonomous: true
requirements:
  - LP-01
  - LP-02

must_haves:
  truths:
    - "Visitor sees competitor price context before seeing the 399 kr/mnd offer"
    - "Consent banner decline and accept buttons have equal visual weight (both solid, same size)"
  artifacts:
    - path: "src/pages/nettside-for-bedrift/_sections/PricingSummary.astro"
      provides: "Competitor price anchor line above the offer card"
      contains: "Andre byråer tar"
    - path: "src/layouts/LandingPageLayout.astro"
      provides: "Equal-prominence consent banner buttons"
      contains: "bg-slate-600"
  key_links:
    - from: "PricingSummary.astro"
      to: "subscriptionOffer config"
      via: "existing import"
      pattern: "import.*subscriptionOffer"
---

<objective>
Add competitor price anchor to PricingSummary and make consent banner buttons equal-prominence.

Purpose: LP-01 frames the 399 kr/mnd offer against competitor pricing (15 000-50 000 kr) so ad traffic immediately sees the value gap. LP-02 ensures GDPR-compliant equal-prominence buttons on the consent banner (no dark patterns).
Output: Two small edits to existing Astro files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-landing-page-ad-consistency/38-CONTEXT.md

<interfaces>
From src/config/subscriptionOffer.ts:
```typescript
export interface SubscriptionOffer {
  price: { setupPrice: number; monthlyPrice: number; };
  features: string[];
  terms: { binding: string; cancellation: string; };
  upsellLinks: Array<{ label: string; href: string; }>;
  meta: { title: string; description: string; };
}
// subscriptionOffer.price.monthlyPrice = 399
```

From src/layouts/LandingPageLayout.astro (lines 170-181, consent buttons):
```html
<button id="cookie-decline"
  class="rounded-full bg-slate-700 px-4 py-2 text-sm font-medium text-text transition-colors hover:bg-slate-600">
  Avslå
</button>
<button id="cookie-accept"
  class="rounded-full bg-brand px-4 py-2 text-sm font-medium text-surface transition-colors hover:bg-brand-light">
  Godta
</button>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add competitor price anchor line in PricingSummary.astro (LP-01)</name>
  <files>src/pages/nettside-for-bedrift/_sections/PricingSummary.astro</files>
  <read_first>
    - src/pages/nettside-for-bedrift/_sections/PricingSummary.astro
    - src/config/subscriptionOffer.ts
  </read_first>
  <action>
    Per D-01, D-02, D-03: Insert a single muted paragraph ABOVE the offer card (before the `<div class="reveal-on-scroll mx-auto max-w-md">` on line 22).

    Add this line inside the existing `<div class="reveal-on-scroll mb-12 text-center">` block, after the existing `<p class="mt-4 text-text-muted">` paragraph (after line 18):

    ```html
    <p class="mt-3 text-sm text-text-muted">
      Andre byraer tar 15 000-50 000 kr for en nettside
    </p>
    ```

    Use the actual Norwegian characters: "Andre byraer tar 15 000-50 000 kr for en nettside" with en-dash between the numbers and proper spacing. The exact text with correct characters: `Andre byråer tar 15 000–50 000 kr for en nettside`

    Styling: `text-sm text-text-muted` -- small and understated per D-02. No badge, no pill, no bold. Just a factual line that sets context before the card.

    Do NOT modify anything else in the file. The price card itself stays untouched.
  </action>
  <verify>
    <automated>grep -c "Andre byråer tar" src/pages/nettside-for-bedrift/_sections/PricingSummary.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Andre byråer tar 15.000.*50.000 kr" src/pages/nettside-for-bedrift/_sections/PricingSummary.astro` returns a match
    - `grep "text-sm text-text-muted" src/pages/nettside-for-bedrift/_sections/PricingSummary.astro` returns at least 2 matches (existing + new line)
    - The new `<p>` tag appears BEFORE the `<div class="reveal-on-scroll mx-auto max-w-md">` line (price card)
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>PricingSummary shows competitor price context ("Andre byraer tar 15 000-50 000 kr for en nettside") in muted text above the offer card</done>
</task>

<task type="auto">
  <name>Task 2: Make consent banner buttons equal-prominence (LP-02)</name>
  <files>src/layouts/LandingPageLayout.astro</files>
  <read_first>
    - src/layouts/LandingPageLayout.astro
  </read_first>
  <action>
    Per D-04, D-05: Change ONLY the class attribute of the `#cookie-decline` button (around line 172). The current class is:

    ```
    rounded-full bg-slate-700 px-4 py-2 text-sm font-medium text-text transition-colors hover:bg-slate-600
    ```

    Change it to:

    ```
    rounded-full bg-slate-600 px-4 py-2 text-sm font-medium text-surface transition-colors hover:bg-slate-500
    ```

    Changes:
    1. `bg-slate-700` becomes `bg-slate-600` (more visible solid background per D-04)
    2. `text-text` becomes `text-surface` (white text to match the accept button's text color, ensuring equal readability)
    3. `hover:bg-slate-600` becomes `hover:bg-slate-500` (hover state one shade lighter than the new base)

    Do NOT change:
    - The `#cookie-accept` button (already correct with `bg-brand`)
    - Banner text, positioning, or JS logic (per D-05)
    - Any other part of the file
  </action>
  <verify>
    <automated>grep "cookie-decline" src/layouts/LandingPageLayout.astro | grep "bg-slate-600"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -A1 "cookie-decline" src/layouts/LandingPageLayout.astro | grep "bg-slate-600"` returns a match
    - `grep -A1 "cookie-decline" src/layouts/LandingPageLayout.astro | grep "text-surface"` returns a match
    - `grep -A1 "cookie-decline" src/layouts/LandingPageLayout.astro | grep "hover:bg-slate-500"` returns a match
    - No occurrences of `bg-slate-700` remain on the decline button line
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Both consent banner buttons are solid with equal visual weight -- Avsla is bg-slate-600 and Godta is bg-brand, both with text-surface for white text</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep "Andre byråer tar" src/pages/nettside-for-bedrift/_sections/PricingSummary.astro` returns a match
3. `grep "bg-slate-600" src/layouts/LandingPageLayout.astro` returns a match on the decline button
</verification>

<success_criteria>
- PricingSummary.astro contains competitor price anchor text in muted styling above the offer card
- LandingPageLayout.astro consent banner has two solid, equal-prominence buttons
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/38-landing-page-ad-consistency/38-01-SUMMARY.md`
</output>
