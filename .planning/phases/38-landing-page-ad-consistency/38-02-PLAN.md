---
phase: 38-landing-page-ad-consistency
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/nettside-for-bedrift/index.astro
autonomous: false
requirements:
  - LP-03

must_haves:
  truths:
    - "Sharing /nettside-for-bedrift on Facebook shows the custom OG image with price offer instead of generic og-image.jpg"
  artifacts:
    - path: "public/images/og-nettside-for-bedrift.jpg"
      provides: "Custom OG image for social sharing"
    - path: "src/pages/nettside-for-bedrift/index.astro"
      provides: "image prop override on LandingPageLayout"
      contains: 'image="/images/og-nettside-for-bedrift.jpg"'
  key_links:
    - from: "src/pages/nettside-for-bedrift/index.astro"
      to: "src/layouts/LandingPageLayout.astro"
      via: "image prop"
      pattern: 'image=.*og-nettside-for-bedrift'
---

<objective>
Wire custom OG image for /nettside-for-bedrift social sharing.

Purpose: When the landing page URL is shared on Facebook/Instagram, the preview should show the subscription offer (0 kr oppstart | 399 kr/mnd) instead of the generic Nettup OG image. This reinforces ad-to-share consistency.
Output: Image file placed in public/ and wired via the existing image prop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-landing-page-ad-consistency/38-CONTEXT.md

<interfaces>
From src/layouts/LandingPageLayout.astro (Props interface):
```typescript
interface Props {
  title: string;
  description?: string;
  image?: string;    // Default: '/images/og-image.jpg'
  noIndex?: boolean;
  phone?: string;
}
```

From src/pages/nettside-for-bedrift/index.astro (current LandingPageLayout usage):
```astro
<LandingPageLayout
  title={subscriptionOffer.meta.title}
  description={subscriptionOffer.meta.description}
>
```
Note: No `image` prop currently passed -- uses default `/images/og-image.jpg`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Place custom OG image file</name>
  <action>
    The user must supply the custom OG image file per D-06 and D-07.

    Spec: 1200x630px JPG, dark background (#020617), Nettup logo (cyan), "0 kr oppstart | 399 kr/mnd" centered in large white/cyan text. Created manually in Figma, Canva, or similar.

    The file must be placed at: `public/images/og-nettside-for-bedrift.jpg`

    If the user has already placed the file, confirm it exists and proceed to Task 2.
    If not, pause and ask the user to create and place it.
  </action>
  <how-to-verify>
    Check that `public/images/og-nettside-for-bedrift.jpg` exists and is a valid JPEG image file.
  </how-to-verify>
  <resume-signal>Type "image placed" when the file is at public/images/og-nettside-for-bedrift.jpg</resume-signal>
  <acceptance_criteria>
    - `ls -la public/images/og-nettside-for-bedrift.jpg` shows a file exists
    - `file public/images/og-nettside-for-bedrift.jpg` reports JPEG image data
  </acceptance_criteria>
  <done>Custom OG image file exists at public/images/og-nettside-for-bedrift.jpg</done>
</task>

<task type="auto">
  <name>Task 2: Wire custom OG image prop in nettside-for-bedrift/index.astro (LP-03)</name>
  <files>src/pages/nettside-for-bedrift/index.astro</files>
  <read_first>
    - src/pages/nettside-for-bedrift/index.astro
    - src/layouts/LandingPageLayout.astro
  </read_first>
  <action>
    Per D-08: Add the `image` prop to the `LandingPageLayout` component in `src/pages/nettside-for-bedrift/index.astro`.

    Change the LandingPageLayout opening tag from:

    ```astro
    <LandingPageLayout
      title={subscriptionOffer.meta.title}
      description={subscriptionOffer.meta.description}
    >
    ```

    To:

    ```astro
    <LandingPageLayout
      title={subscriptionOffer.meta.title}
      description={subscriptionOffer.meta.description}
      image="/images/og-nettside-for-bedrift.jpg"
    >
    ```

    This overrides the default `/images/og-image.jpg` with the custom landing page OG image. The `image` prop is already wired through to the `og:image` meta tag in LandingPageLayout.astro -- no other changes needed.
  </action>
  <verify>
    <automated>grep 'image="/images/og-nettside-for-bedrift.jpg"' src/pages/nettside-for-bedrift/index.astro</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'image="/images/og-nettside-for-bedrift.jpg"' src/pages/nettside-for-bedrift/index.astro` returns exactly one match
    - `npm run build` completes without errors
    - The built HTML for /nettside-for-bedrift contains `og:image` with `og-nettside-for-bedrift.jpg` in the content attribute
  </acceptance_criteria>
  <done>The /nettside-for-bedrift page passes image="/images/og-nettside-for-bedrift.jpg" to LandingPageLayout, overriding the default OG image</done>
</task>

</tasks>

<verification>
1. `ls public/images/og-nettside-for-bedrift.jpg` -- file exists
2. `grep 'og-nettside-for-bedrift' src/pages/nettside-for-bedrift/index.astro` -- prop is wired
3. `npm run build` -- completes without errors
</verification>

<success_criteria>
- Custom OG image file exists at public/images/og-nettside-for-bedrift.jpg
- index.astro passes the custom image path to LandingPageLayout
- Build passes and the og:image meta tag in the built HTML references the custom image
</success_criteria>

<output>
After completion, create `.planning/phases/38-landing-page-ad-consistency/38-02-SUMMARY.md`
</output>
