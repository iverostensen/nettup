---
phase: 41-lead-magnet-mid-funnel-asset
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/Footer.astro
autonomous: false
requirements:
  - LEAD-01

must_haves:
  truths:
    - "/sjekkliste is discoverable from the footer on every page"
  artifacts:
    - path: "src/components/layout/Footer.astro"
      provides: "Footer with /sjekkliste link in Sider nav"
      contains: "/sjekkliste"
  key_links:
    - from: "src/components/layout/Footer.astro"
      to: "/sjekkliste"
      via: "anchor tag in navLinks array"
      pattern: "sjekkliste"
---

<objective>
Add /sjekkliste entry point to the site footer and verify the complete lead magnet page visually.

Purpose: The footer link is the only entry point for the lead magnet page (per D-08). Without it, users cannot discover the checklist.
Output: Footer link added, visual verification of the full /sjekkliste flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/41-lead-magnet-mid-funnel-asset/41-CONTEXT.md
@.planning/phases/41-lead-magnet-mid-funnel-asset/41-01-SUMMARY.md

<interfaces>
From src/components/layout/Footer.astro (navLinks array):
```typescript
const navLinks = [
  { name: 'Hjem', href: '/' },
  { name: 'Tjenester', href: '/tjenester' },
  { name: 'Om oss', href: '/om-oss' },
  { name: 'Prosjekter', href: '/prosjekter' },
  { name: 'Kontakt', href: '/kontakt' },
  { name: 'Blogg', href: '/blogg' },
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /sjekkliste link to footer navigation</name>
  <files>src/components/layout/Footer.astro</files>
  <read_first>
    - src/components/layout/Footer.astro (current footer with navLinks array and 4-column grid)
  </read_first>
  <action>
Add "Gratis sjekkliste" to the `navLinks` array in `src/components/layout/Footer.astro` (per D-08: footer link only). Append as the last entry in the existing `navLinks` array -- do NOT create a new footer column for a single link.

Change the navLinks array from:
```typescript
const navLinks = [
  { name: 'Hjem', href: '/' },
  { name: 'Tjenester', href: '/tjenester' },
  { name: 'Om oss', href: '/om-oss' },
  { name: 'Prosjekter', href: '/prosjekter' },
  { name: 'Kontakt', href: '/kontakt' },
  { name: 'Blogg', href: '/blogg' },
];
```

To:
```typescript
const navLinks = [
  { name: 'Hjem', href: '/' },
  { name: 'Tjenester', href: '/tjenester' },
  { name: 'Om oss', href: '/om-oss' },
  { name: 'Prosjekter', href: '/prosjekter' },
  { name: 'Kontakt', href: '/kontakt' },
  { name: 'Blogg', href: '/blogg' },
  { name: 'Gratis sjekkliste', href: '/sjekkliste' },
];
```

The link text is "Gratis sjekkliste" (per UI-SPEC copywriting contract). It renders with the existing `link-underline flex min-h-11 w-fit items-center text-sm text-text-muted transition-colors duration-200 hover:text-brand` classes via the navLinks map. No additional styling needed.
  </action>
  <verify>
    <automated>grep "sjekkliste" /Users/iverostensen/nettup/src/components/layout/Footer.astro</automated>
  </verify>
  <acceptance_criteria>
    - Footer.astro navLinks array contains `{ name: 'Gratis sjekkliste', href: '/sjekkliste' }`
    - No new grid column added (grid stays `grid-cols-2 md:grid-cols-2 lg:grid-cols-4`)
    - navLinks array has exactly 7 entries (6 existing + 1 new)
  </acceptance_criteria>
  <done>Footer "Sider" column includes "Gratis sjekkliste" link pointing to /sjekkliste</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of /sjekkliste page</name>
  <what-built>
    Complete /sjekkliste lead magnet page with:
    - 10-item checklist (3 visible, 7 blurred with lock icon)
    - 2-field email gate form (navn, e-post)
    - Blur-to-reveal animation on form submit
    - CTA card after unlock linking to /kontakt
    - Footer link entry point
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:4321/sjekkliste
    2. Verify items 1-3 are visible with cyan number badges, items 4-10 are blurred
    3. Verify lock icon and gate form card overlays the blurred area
    4. Submit the form with a test name and email (note: Formspree ID must be configured first -- if placeholder, check that form UI works correctly up to the POST)
    5. After successful submit: verify blur removes smoothly, items 4-10 appear with staggered animation
    6. Verify CTA card appears below checklist: "Vil du at vi fikser dette for deg?" with "Ta kontakt" button
    7. Click "Ta kontakt" -- should navigate to /kontakt?tjeneste=nettside
    8. Scroll to footer on any page -- verify "Gratis sjekkliste" link appears in Sider column
    9. Test on mobile viewport (375px) -- verify single column layout, form fields stack vertically
    10. Test with browser `prefers-reduced-motion: reduce` -- verify instant reveal with no animation
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `grep "sjekkliste" src/components/layout/Footer.astro` shows the link
2. `npm run build` passes cleanly
3. Visual inspection of /sjekkliste confirms full flow works
</verification>

<success_criteria>
- Footer shows "Gratis sjekkliste" link on every page
- /sjekkliste page looks correct on mobile and desktop
- Form gate + reveal animation works as designed
- User approves visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/41-lead-magnet-mid-funnel-asset/41-02-SUMMARY.md`
</output>
