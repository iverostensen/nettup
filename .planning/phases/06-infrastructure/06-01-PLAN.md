---
phase: 06-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/services.ts
autonomous: true
requirements:
  - CONFIG-01
  - CONFIG-02

must_haves:
  truths:
    - "src/config/services.ts exists and exports both the Service interface and the services array"
    - "All 7 services are defined: nettside, nettbutikk, landingsside, webapp, seo, ai, vedlikehold"
    - "Each service object has slug, name, tagline, priceRange, minPrice, maxPrice, ctaParam, and description fields"
    - "TypeScript strict mode compiles without errors"
  artifacts:
    - path: "src/config/services.ts"
      provides: "Single source of truth for all service metadata"
      exports: ["Service", "services"]
  key_links:
    - from: "src/components/islands/FloatingNav.tsx"
      to: "src/config/services.ts"
      via: "import { services } from '@/config/services'"
      pattern: "services\\.find"
    - from: "src/pages/kontakt/_sections/ContactForm.tsx"
      to: "src/config/services.ts"
      via: "import { services } from '@/config/services'"
      pattern: "services\\.map"
---

<objective>
Create the services.ts config file that acts as the single source of truth for all 7 Nettup service offerings.

Purpose: Every downstream component in Phase 6 and beyond imports from this file — slugs, display names, price ranges, and CTA params. Creating it first removes the dependency blocker for all other Phase 6 tasks.
Output: src/config/services.ts with exported Service interface and services array.
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-infrastructure/06-CONTEXT.md

<interfaces>
<!-- Template to follow exactly: src/config/pricing.ts -->
```typescript
export interface Pakke {
  id: 'enkel' | 'standard' | 'premium';
  name: string;
  description: string;
  originalPrice: string;
  // ... other fields
}

export const pakker: Pakke[] = [
  { id: 'enkel', name: 'Enkel', ... },
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/config/services.ts</name>
  <files>src/config/services.ts</files>
  <action>
Create `/Users/iverostensen/nettup/src/config/services.ts` following the exact same pattern as `src/config/pricing.ts` (interface + named export array).

**Service interface fields (all required):**
- `slug: string` — URL segment used in `/tjenester/[slug]`
- `name: string` — Norwegian display name shown in nav, badges, breadcrumbs
- `tagline: string` — One-line value proposition (Norwegian)
- `priceRange: string` — Human-readable display string, e.g. "fra 15 000 kr"
- `minPrice: number` — Numeric minimum for JSON-LD PriceSpecification (Phase 10)
- `maxPrice: number` — Numeric maximum for JSON-LD PriceSpecification (Phase 10); use 0 if open-ended
- `ctaParam: string` — Same as slug; the value used in `?tjeneste=[ctaParam]` CTA links
- `description: string` — One-sentence outcome statement for service cards (Phase 7)

**All 7 services to define:**

| slug | name | priceRange | minPrice | maxPrice |
|------|------|-----------|----------|----------|
| nettside | Nettside | fra 15 000 kr | 15000 | 0 |
| nettbutikk | Nettbutikk | fra 25 000 kr | 25000 | 0 |
| landingsside | Landingsside | fra 8 000 kr | 8000 | 0 |
| webapp | Webapp | fra 40 000 kr | 40000 | 0 |
| seo | SEO | fra 3 000 kr/mnd | 3000 | 0 |
| ai | AI-løsning | fra 20 000 kr | 20000 | 0 |
| vedlikehold | Vedlikehold | fra 1 500 kr/mnd | 1500 | 0 |

Write appropriate Norwegian taglines and descriptions for each service. Keep taglines under 10 words. Keep descriptions one sentence focused on customer outcome (not technical features).

Do NOT copy the `priceRange` display strings verbatim from the table — write them as natural Norwegian (e.g. "fra 15 000 kr" is fine, "3 000 kr/mnd" for recurring services).
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- File exists at src/config/services.ts
- TypeScript compiles without errors
- `services` array has exactly 7 entries
- All 7 slugs present: nettside, nettbutikk, landingsside, webapp, seo, ai, vedlikehold
- Both `Service` interface and `services` const are exported
  </done>
</task>

</tasks>

<verification>
Run `cd /Users/iverostensen/nettup && npx tsc --noEmit` — zero TypeScript errors.
Run `node -e "const s = require('./src/config/services.ts'); console.log(s.services.length)"` is not applicable for TS directly, but the tsc check is sufficient.
</verification>

<success_criteria>
- services.ts exists with exported Service interface and services array
- All 7 services defined with all required fields
- TypeScript strict mode: no errors
- Slug list: nettside, nettbutikk, landingsside, webapp, seo, ai, vedlikehold
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure/06-01-SUMMARY.md`
</output>
