---
phase: 01-brand-identity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/brand.ts
  - tailwind.config.ts
autonomous: true
requirements:
  - BRAND-02

must_haves:
  truths:
    - "A single TypeScript file (src/config/brand.ts) is the source of truth for all design tokens"
    - "Tailwind generates utility classes from brand tokens — font-display, rounded-sm/md/lg, duration-fast/normal/slow, delay-1 through delay-5"
    - "Build passes without errors after integration"
    - "Existing color tokens (brand, surface, text) are preserved unchanged"
  artifacts:
    - path: "src/config/brand.ts"
      provides: "All design tokens as a typed TypeScript const"
      exports: ["brand", "Brand"]
    - path: "tailwind.config.ts"
      provides: "Tailwind config consuming brand tokens"
      contains: "import { brand } from './src/config/brand'"
  key_links:
    - from: "src/config/brand.ts"
      to: "tailwind.config.ts"
      via: "import { brand } from './src/config/brand'"
      pattern: "import.*brand.*from.*config/brand"
    - from: "tailwind.config.ts"
      to: "src/styles/global.css"
      via: "@config '../../tailwind.config.ts'"
      pattern: "@config"
---

<objective>
Create `src/config/brand.ts` with all design tokens and wire it into `tailwind.config.ts`.

Purpose: This is the technical foundation of BRAND-02. Without it, no component can reference token-based classes. Plans 03 and 04 depend on the Tailwind utilities this plan generates.
Output: `src/config/brand.ts` (new), `tailwind.config.ts` (updated with import and token extensions).
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-brand-identity/01-CONTEXT.md
@.planning/phases/01-brand-identity/01-RESEARCH.md
@tailwind.config.ts
@src/styles/global.css

<interfaces>
<!-- Current tailwind.config.ts structure. Executor extends this — do not replace colors. -->
```typescript
// tailwind.config.ts (current)
import type { Config } from 'tailwindcss';

export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
  theme: {
    extend: {
      colors: {
        brand: { DEFAULT: '#06b6d4', light: '#22d3ee', dark: '#0891b2' },
        surface: { DEFAULT: '#020617', raised: '#0F172A' },
        text: { DEFAULT: '#F8FAFC', muted: '#94A3B8' },
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
      },
    },
  },
  plugins: [],
} satisfies Config;
```

<!-- global.css line 2 — confirms @config directive is active and working -->
@config '../../tailwind.config.ts';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/config/brand.ts</name>
  <files>src/config/brand.ts</files>
  <action>
Create a strongly-typed TypeScript const that exports all design tokens. This is the single source of truth — components reference Tailwind classes derived from these tokens, not this file directly.

```typescript
// src/config/brand.ts

export const brand = {
  fonts: {
    display: '"Space Grotesk", system-ui, sans-serif',
    body: 'Inter, system-ui, sans-serif',
  },
  fontWeight: {
    h1: '700',
    h2: '600',
  },
  radius: {
    sm: '0.375rem',   // 6px — badges, tags
    md: '0.75rem',    // 12px — cards, inputs (matches Tailwind rounded-xl = 0.75rem)
    lg: '1.5rem',     // 24px — larger containers
    full: '9999px',   // buttons
  },
  duration: {
    fast: '150ms',
    normal: '300ms',
    slow: '500ms',
  },
  easing: {
    default: 'ease-out',
    snappy: 'cubic-bezier(0.34, 1.56, 0.64, 1)',
    gentle: 'cubic-bezier(0.4, 0, 0.2, 1)',
  },
  delay: {
    1: '100ms',
    2: '200ms',
    3: '300ms',
    4: '400ms',
    5: '500ms',
  },
} as const;

export type Brand = typeof brand;
```

Note on radius.md: Set to `0.75rem` to match the existing `rounded-xl` Tailwind value used on Card.astro — preserves visual consistency. radius.lg (`1.5rem`) is for future larger containers.

Note on fonts.display: Use double quotes around "Space Grotesk" since it contains a space.
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && npx tsc --noEmit --project tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>
    src/config/brand.ts exists, exports `brand` const and `Brand` type, TypeScript strict mode passes (no `any`, no type errors).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update tailwind.config.ts to consume brand tokens</name>
  <files>tailwind.config.ts</files>
  <action>
Import brand from brand.ts and extend the Tailwind config with token-derived utilities. Preserve ALL existing color tokens exactly as-is.

The updated tailwind.config.ts should look like:

```typescript
import type { Config } from 'tailwindcss';
import { brand } from './src/config/brand';

export default {
  content: ['./src/**/*.{astro,html,js,jsx,md,mdx,svelte,ts,tsx,vue}'],
  theme: {
    extend: {
      colors: {
        brand: { DEFAULT: '#06b6d4', light: '#22d3ee', dark: '#0891b2' },
        surface: { DEFAULT: '#020617', raised: '#0F172A' },
        text: { DEFAULT: '#F8FAFC', muted: '#94A3B8' },
      },
      fontFamily: {
        display: [brand.fonts.display],   // → font-display class (Space Grotesk)
        sans: [brand.fonts.body],         // → font-sans (unchanged, Inter)
      },
      borderRadius: {
        sm: brand.radius.sm,
        md: brand.radius.md,
        lg: brand.radius.lg,
        full: brand.radius.full,
      },
      transitionDuration: {
        fast: brand.duration.fast,
        normal: brand.duration.normal,
        slow: brand.duration.slow,
      },
      transitionTimingFunction: {
        snappy: brand.easing.snappy,
        gentle: brand.easing.gentle,
      },
      transitionDelay: {
        1: brand.delay[1],
        2: brand.delay[2],
        3: brand.delay[3],
        4: brand.delay[4],
        5: brand.delay[5],
      },
    },
  },
  plugins: [],
} satisfies Config;
```

Key points:
- The `fontFamily.display` key generates the `font-display` Tailwind utility class (applies `font-family: "Space Grotesk"`). This is a font-family utility, not the CSS `font-display` property — separate namespaces in Tailwind.
- Do NOT add `transitionTimingFunction` for `easing.default` ('ease-out') — that's a CSS keyword, not a custom value that needs registration.
- The `@config` directive in global.css already points to this file — no changes needed to global.css in this plan.
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
    tailwind.config.ts imports brand, extends Tailwind with fontFamily.display, borderRadius sm/md/lg/full, transitionDuration fast/normal/slow, transitionTimingFunction snappy/gentle, transitionDelay 1-5. Build passes. Running `npm run build` completes without errors.
  </done>
</task>

</tasks>

<verification>
Run `npm run build` — must complete without errors. Then verify generated CSS contains expected token classes by checking build output exists:
- `dist/` directory created
- Build output shows pages generated (should be 7 pages as before)
</verification>

<success_criteria>
BRAND-02 (foundation): `src/config/brand.ts` exists as the typed token source. `tailwind.config.ts` imports it and generates utility classes for display font, border radius, transition duration/delay/easing. Build passes clean. This plan creates the token system — Plans 03 and 04 apply it to components.
</success_criteria>

<output>
After completion, create `.planning/phases/01-brand-identity/01-02-SUMMARY.md` with:
- Confirmation that build passes
- Token values chosen (especially radius.md = 0.75rem decision)
- Any TypeScript issues encountered and resolved
- Tailwind classes now available for downstream plans: font-display, rounded-sm/md/lg, duration-fast/normal/slow, delay-1 through delay-5, ease-snappy/gentle
</output>
