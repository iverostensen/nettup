---
phase: 01-brand-identity
plan: 04
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - src/components/ui/Button.astro
  - src/components/ui/Card.astro
  - src/components/ui/SectionHeader.astro
  - src/styles/global.css
autonomous: false
requirements:
  - BRAND-02

must_haves:
  truths:
    - "UI components (Button, Card, SectionHeader) reference token-based Tailwind classes instead of hardcoded values"
    - "reveal-delay-1 through reveal-delay-6 CSS classes in global.css are replaced by the Tailwind delay-1 through delay-5 token utilities"
    - "SectionHeader H2 uses font-display class (Space Grotesk, weight 600 — semibold)"
    - "Build passes and site renders correctly — no visual regressions from the token migration"
    - "Card radius does not visually change (radius.md = 0.75rem matches old rounded-xl)"
  artifacts:
    - path: "src/components/ui/Button.astro"
      provides: "Button using duration-fast token"
      contains: "duration-fast"
    - path: "src/components/ui/Card.astro"
      provides: "Card using duration-normal token and rounded-md"
      contains: "rounded-md"
    - path: "src/components/ui/SectionHeader.astro"
      provides: "H2 with font-display and font-semibold"
      contains: "font-display font-semibold"
    - path: "src/styles/global.css"
      provides: "No reveal-delay CSS classes (replaced by Tailwind tokens)"
  key_links:
    - from: "src/config/brand.ts delay tokens"
      to: "src/styles/global.css reveal-delay-* usages"
      via: "Tailwind delay-1 through delay-5 replacing .reveal-delay-1 through .reveal-delay-6"
      pattern: "delay-[1-5]"
    - from: "src/config/brand.ts duration.fast"
      to: "Button.astro transition"
      via: "duration-fast Tailwind class (150ms)"
      pattern: "duration-fast"
---

<objective>
Migrate UI components to use brand token Tailwind classes and clean up replaced CSS delay utilities.

Purpose: This plan completes BRAND-02 — the token system is only complete when components actually consume it. After this plan, hardcoded `duration-200`, `rounded-xl`, and CSS `reveal-delay-*` classes are replaced by token-derived classes. Depends on Plan 02 having registered the token utilities.
Output: Updated Button.astro, Card.astro, SectionHeader.astro, global.css.
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-brand-identity/01-02-SUMMARY.md
@.planning/phases/01-brand-identity/01-CONTEXT.md
@.planning/phases/01-brand-identity/01-RESEARCH.md
@src/components/ui/Button.astro
@src/components/ui/Card.astro
@src/components/ui/SectionHeader.astro
@src/styles/global.css

<interfaces>
<!-- Current component classes to migrate -->

Button.astro baseClasses (line 18):
'inline-flex items-center justify-center font-semibold rounded-full transition-all duration-200'
→ duration-200 becomes duration-fast (brand.duration.fast = 150ms — intentional, close enough)
→ rounded-full stays (maps to brand.radius.full = 9999px — same value)

Card.astro classes (lines 27-32):
'rounded-xl border border-white/10 bg-surface-raised'
hover: 'transition-all duration-300 hover:border-brand/30 hover:-translate-y-1 hover:shadow-xl hover:shadow-brand/5'
→ rounded-xl becomes rounded-md (brand.radius.md = 0.75rem = same visual as Tailwind rounded-xl)
→ duration-300 becomes duration-normal (brand.duration.normal = 300ms = identical value)

SectionHeader.astro H2 (line 13):
class="reveal-on-scroll text-3xl font-bold md:text-4xl"
→ Add font-display (Space Grotesk)
→ Change font-bold to font-semibold (H2 = weight 600 per CONTEXT.md)

global.css reveal-delay classes (lines 135-140):
.reveal-delay-1 { transition-delay: 100ms; }
.reveal-delay-2 { transition-delay: 200ms; }
.reveal-delay-3 { transition-delay: 300ms; }
.reveal-delay-4 { transition-delay: 400ms; }
.reveal-delay-5 { transition-delay: 500ms; }
.reveal-delay-6 { transition-delay: 600ms; }
→ Remove these 6 classes. They are replaced by Tailwind's delay-1 through delay-5 utilities (from brand.delay tokens).
→ reveal-delay-6 (600ms): check if used anywhere before removing. If found, add delay.6 to brand.ts first.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Button.astro and Card.astro to token classes</name>
  <files>src/components/ui/Button.astro, src/components/ui/Card.astro</files>
  <action>
**Button.astro:**
Change `baseClasses` from:
`'inline-flex items-center justify-center font-semibold rounded-full transition-all duration-200'`
To:
`'inline-flex items-center justify-center font-semibold rounded-full transition-all duration-fast'`

Only change: `duration-200` → `duration-fast`. Keep `rounded-full` — it already maps to brand.radius.full.

**Card.astro:**
Change the base classes string from:
`'rounded-xl border border-white/10 bg-surface-raised'`
To:
`'rounded-md border border-white/10 bg-surface-raised'`

Change the hover string from:
`'transition-all duration-300 hover:border-brand/30 hover:-translate-y-1 hover:shadow-xl hover:shadow-brand/5'`
To:
`'transition-all duration-normal hover:border-brand/30 hover:-translate-y-1 hover:shadow-xl hover:shadow-brand/5'`

Changes: `rounded-xl` → `rounded-md` (same visual: both 0.75rem), `duration-300` → `duration-normal` (same value: 300ms).
  </action>
  <verify>
    <automated>grep "duration-fast" /Users/iverostensen/nettup/src/components/ui/Button.astro && grep "rounded-md" /Users/iverostensen/nettup/src/components/ui/Card.astro && grep "duration-normal" /Users/iverostensen/nettup/src/components/ui/Card.astro && echo "token migration confirmed"</automated>
  </verify>
  <done>
    Button.astro uses `duration-fast`. Card.astro uses `rounded-md` and `duration-normal`. No `duration-200` or `rounded-xl` remain in these files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate SectionHeader H2 and clean up global.css delay classes</name>
  <files>src/components/ui/SectionHeader.astro, src/styles/global.css</files>
  <action>
**SectionHeader.astro — H2 update:**
Change line ~13 from:
`<h2 class="reveal-on-scroll text-3xl font-bold md:text-4xl">`
To:
`<h2 class="reveal-on-scroll font-display text-3xl font-semibold md:text-4xl">`

Two changes:
1. Add `font-display` — applies Space Grotesk to all H2 headings site-wide (SectionHeader is the shared H2 component)
2. Change `font-bold` to `font-semibold` — H2 weight is 600 per CONTEXT.md locked decision

Update the subtitle `<p>` tag on line ~17:
`class="reveal-on-scroll reveal-delay-1 mx-auto mt-4 max-w-2xl text-lg text-text-muted"`
→ `class="reveal-on-scroll delay-1 mx-auto mt-4 max-w-2xl text-lg text-text-muted"`

**global.css — remove reveal-delay classes:**
Before removing, run: `grep -rn "reveal-delay-6" /Users/iverostensen/nettup/src/`
- If reveal-delay-6 is found: add `delay: { 6: '600ms' }` to brand.ts and corresponding entry in tailwind.config.ts transitionDelay
- Then remove all 6 `.reveal-delay-*` class definitions from global.css (lines 135-140)

**Search and replace reveal-delay-* across all source files:**
Replace every occurrence of `reveal-delay-N` with `delay-N` (N = 1 through 5).
For dynamic template literals like `` `reveal-delay-${index + 1}` `` → replace with `` `delay-${index + 1}` ``.

Files known to use reveal-delay-* (update all of these):
- src/pages/nettside-for-bedrift/_sections/Results.astro
- src/pages/nettside-for-bedrift/_sections/WhyUs.astro
- src/pages/nettside-for-bedrift/_sections/Testimonial.astro
- src/pages/nettside-for-bedrift/_sections/VisualProof.astro
- src/pages/nettside-for-bedrift/_sections/FormSection.astro
- src/pages/nettside-for-bedrift/_sections/FAQ.astro (dynamic: `reveal-delay-${(index % 3) + 1}`)
- src/pages/_home/ProjectTeaser.astro
- src/pages/_home/WhyNettup.astro (dynamic: `reveal-delay-${index + 1}`)
- src/pages/_home/Process.astro (dynamic: `reveal-delay-${index + 1}`)
- src/pages/_home/Problem.astro (dynamic: `reveal-delay-${index + 1}`)
- src/pages/om-oss/_sections/Approach.astro
- src/pages/om-oss/_sections/Values.astro (dynamic: `reveal-delay-${index + 1}`)
- src/pages/tjenester/_sections/Support.astro (dynamic: `reveal-delay-${index + 1}`)
- src/pages/tjenester/_sections/Inkludert.astro (dynamic: `reveal-delay-${(index % 3) + 1}`)
- src/pages/tjenester/_sections/Pakker.astro (dynamic: `reveal-delay-${index + 1}`)
- src/pages/_home/Solution.astro
- src/pages/prosjekter/_sections/Results.astro
- src/pages/prosjekter/_sections/ProjectShowcase.astro
- src/pages/kontakt/index.astro
- src/pages/kontakt/_sections/AlternativeContact.astro
- src/pages/nettside-for-bedrift/_sections/Hero.astro
- src/pages/nettside-for-bedrift/_sections/PricingSummary.astro

After all replacements, run build to confirm no class generation issues.
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && grep -rn "reveal-delay-" src/ --include="*.astro" --include="*.tsx" --include="*.ts" | grep -v "global.css" | wc -l && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    SectionHeader.astro H2 uses `font-display font-semibold`. global.css has no `.reveal-delay-*` classes. All usages of `reveal-delay-*` in src/ replaced with `delay-*`. Grep count returns 0. Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete brand token migration: brand.ts tokens are now consumed by UI components. Button uses duration-fast, Card uses rounded-md and duration-normal, SectionHeader H2 uses font-display (Space Grotesk) and font-semibold. Scroll stagger delays migrated from CSS classes to Tailwind token utilities across 20+ files.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and visit each page:

    1. http://localhost:4321/ — Homepage
       - Section headers (e.g. "Hva vi tilbyr") should use Space Grotesk, slightly lighter weight than before (semibold instead of bold)
       - Scroll down — stagger animations on cards/lists should still work with delays
       - Hover a card — should still lift with transition

    2. http://localhost:4321/tjenester — Services
       - Pricing cards should look identical in shape (rounded-md = same radius as before)
       - Section header font should be Space Grotesk

    3. http://localhost:4321/prosjekter — Portfolio
       - Cards should render correctly, same radius as before

    4. Any page with a primary Button — hover it — transition should still feel smooth

    Expected: Everything looks identical or slightly more refined. No layout breaks. Stagger animations still work.
    Report any visual regression (unexpected shape changes, missing animations, layout shifts).
  </how-to-verify>
  <resume-signal>Type "approved" if all looks correct, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors — critical check for widespread delay class renames
2. `grep -rn "reveal-delay-" src/` — returns 0 results (or only in comments)
3. `grep "font-display" src/components/ui/SectionHeader.astro` — returns match
4. `grep "font-semibold" src/components/ui/SectionHeader.astro` — returns match (no font-bold on h2)
5. `grep "duration-fast" src/components/ui/Button.astro` — returns match
6. Human visual verification — no regressions on stagger animations or card shapes
</verification>

<success_criteria>
BRAND-02 (complete): The token system is not only defined in brand.ts but actively consumed by UI components. Components use duration-fast, duration-normal, rounded-md instead of hardcoded values. SectionHeader H2 is visually differentiated with Space Grotesk semibold. The CSS layer no longer owns delay values — Tailwind does. Build passes clean. Human verifies no visual regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-brand-identity/01-04-SUMMARY.md` with:
- Confirmation build passes
- Whether reveal-delay-6 was found (and how handled)
- Count of files updated in reveal-delay migration
- Human verification result and any issues noted
</output>
