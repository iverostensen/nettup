---
phase: 21-konfig-og-indeks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/projects.ts
  - src/layouts/BaseLayout.astro
  - src/pages/_home/ProjectTeaser.astro
autonomous: true
requirements:
  - INFR-01
  - INFR-04

must_haves:
  truths:
    - "`Project` interface includes `slug`, `techStack`, `publishedAt`, `metaTitle`, `metaDescription`, and optional `testimonialId`, `metrics`, `gallery` fields"
    - "`caseStudySection` flag is absent from the interface and the iGive project entry"
    - "Blom Company project entry exists in `projects` array with all required fields populated"
    - "`pageLabels` in BaseLayout contains entries for `/prosjekter/igive` and `/prosjekter/blom-company`"
    - "`ProjectTeaser.astro` on homepage links to `/prosjekter/igive`"
    - "`npm run build` passes after all changes"
  artifacts:
    - path: "src/config/projects.ts"
      provides: "Extended Project interface + both project entries"
      contains: "slug"
    - path: "src/layouts/BaseLayout.astro"
      provides: "pageLabels with case study routes"
      contains: "/prosjekter/igive"
    - path: "src/pages/_home/ProjectTeaser.astro"
      provides: "Homepage project teaser linking to igive case study"
      contains: "/prosjekter/igive"
  key_links:
    - from: "src/config/projects.ts"
      to: "src/pages/prosjekter/_sections/ProjectGrid.astro (Phase 21-02)"
      via: "projects array export + slug field"
      pattern: "export const projects"
    - from: "src/layouts/BaseLayout.astro"
      to: "BreadcrumbList JSON-LD"
      via: "pageLabels Record lookup"
      pattern: "pageLabels\\[fullPath\\]"
---

<objective>
Extend the `projects.ts` data layer with the fields required by case study pages, add the Blom Company project entry, and wire up the two consumer updates that are independent of the new grid component.

Purpose: Establishes the stable data contract that Plan 21-02 (ProjectGrid) and Phase 22 (case study pages) will build against. All three changes are config/wiring-only — no new components.
Output: Updated `projects.ts`, `BaseLayout.astro` pageLabels, `ProjectTeaser.astro` link target.
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Current projects.ts interface — replace entirely per task action -->
```typescript
// src/config/projects.ts (CURRENT — to be extended)
import type { ImageMetadata } from 'astro';

export interface Project {
  id: string;
  name: string;
  category: string;
  type: string;
  tagline: string;
  description: string;
  image: ImageMetadata;
  imageAlt: string;
  url?: string;
  caseStudySection?: boolean;   // REMOVE THIS
  comingSoon?: boolean;
  challenge?: string;
  solution?: string;
  features?: string[];
  photoUrl?: string;
}

export const projects: Project[] = [
  {
    id: 'igive',
    name: 'iGive',
    category: 'B2B',
    type: 'Gavekort-plattform',
    tagline: 'Profesjonell salgsside for Norges ledende gavekortplattform',
    description: '...',
    image: iGiveImage,
    imageAlt: 'Skjermbilde av salg.igive.no',
    url: 'https://salg.igive.no',
    caseStudySection: true,   // REMOVE
    challenge: '...',
    solution: '...',
    features: [...],
  },
];
```

<!-- Current BaseLayout.astro pageLabels Record -->
```typescript
// src/layouts/BaseLayout.astro (CURRENT)
const pageLabels: Record<string, string> = {
  '/': 'Hjem',
  '/tjenester': 'Tjenester',
  '/tjenester/nettside': 'Nettside',
  '/tjenester/nettbutikk': 'Nettbutikk',
  '/tjenester/landingsside': 'Landingsside',
  '/om-oss': 'Om oss',
  '/prosjekter': 'Prosjekter',
  '/kontakt': 'Kontakt',
  '/priskalkulator': 'Prisestimator',
  '/blogg': 'Blogg',
};
// ADD: '/prosjekter/igive': 'iGive', '/prosjekter/blom-company': 'Blom Company'
```

<!-- Current ProjectTeaser.astro link target -->
```astro
<!-- src/pages/_home/ProjectTeaser.astro (CURRENT) -->
<a href="/prosjekter" ...>   <!-- CHANGE TO: href="/prosjekter/igive" -->
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend projects.ts interface and add Blom Company entry</name>
  <files>src/config/projects.ts</files>
  <action>
Rewrite `src/config/projects.ts` with the extended `Project` interface and both project entries.

**Interface changes:**
- Add required fields: `slug: string`, `techStack: string[]`, `publishedAt: string`, `metaTitle: string`, `metaDescription: string`
- Add optional fields: `testimonialId?: string`, `metrics?: { performance?: number; accessibility?: number; bestPractices?: number; seo?: number }`, `gallery?: { src: ImageMetadata; alt: string }[]`
- Remove `caseStudySection?: boolean` — delete from interface entirely
- Keep `comingSoon?: boolean` — still harmless and may be useful (Claude's discretion per CONTEXT.md)
- Keep existing optional case study fields: `challenge?`, `solution?`, `features?` — Phase 22 will use them

**iGive entry updates:**
- Add `slug: 'igive'`
- Add `techStack: ['Astro', 'Tailwind CSS', 'Vercel']`
- Add `publishedAt: '2024-01-01'`
- Add `metaTitle: 'iGive – Gavekortplattform for bedrifter | Nettup'`
- Add `metaDescription: 'Nettup bygget en dedikert salgsside for iGive som hjelper bedrifter med å tilby gavekort. Rask, tydelig og synlig i Google.'`
- Add `metrics: { performance: 96, accessibility: 96, bestPractices: 100, seo: 100 }` (measured 2026-03-07, recorded in STATE.md)
- Remove `caseStudySection: true`

**Blom Company entry — add as second item in `projects` array:**
- `id: 'blom-company'`
- `slug: 'blom-company'`
- `name: 'Blom Company'`
- `category: 'B2C'`
- `type: 'Nettbutikk'`
- `tagline: 'Eksklusiv nettbutikk for livsstil og golfklær'`
- `description: 'En eksklusiv nettbutikk for et norsk livsstils- og golfklesmerke. Bygget med Next.js 15, Shopify og Sanity for rask og fleksibel e-handel.'`
- `image: blomImage` (import from `@/assets/images/blom-hero.png`)
- `imageAlt: 'Skjermbilde av Blom Company nettbutikk'`
- `url: 'https://blom-no.vercel.app'`
- `slug: 'blom-company'`
- `techStack: ['Next.js 15', 'Shopify', 'Sanity', 'Tailwind CSS 4', 'Vercel']`
- `publishedAt: '2024-01-01'`
- `metaTitle: 'Blom Company – Nettbutikk for livsstil og golfklær | Nettup'`
- `metaDescription: 'Nettup bygget en eksklusiv nettbutikk for Blom Company med Next.js 15, Shopify og Sanity. Rask, visuell og klar for vekst.'`
- `metrics: { performance: 99, accessibility: 96, bestPractices: 100, seo: 100 }` (measured 2026-03-07, STATE.md)
- Leave `challenge`, `solution`, `features`, `gallery`, `testimonialId` absent — Phase 22 will populate

Add image import at top of file: `import blomImage from '@/assets/images/blom-hero.png';`
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build passes. `projects` array has two entries. `Project` interface has `slug`, `techStack`, `metrics`, `gallery`, `testimonialId`, `metaTitle`, `metaDescription`, `publishedAt`. `caseStudySection` is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Add pageLabels entries + update ProjectTeaser link</name>
  <files>src/layouts/BaseLayout.astro, src/pages/_home/ProjectTeaser.astro</files>
  <action>
**BaseLayout.astro:** In the `pageLabels` Record, add two new entries after `/prosjekter`:
```
'/prosjekter/igive': 'iGive',
'/prosjekter/blom-company': 'Blom Company',
```
No other changes to BaseLayout.astro.

**ProjectTeaser.astro:** Change the single `href="/prosjekter"` on the `<a>` tag to `href="/prosjekter/igive"`. No other changes — the component remains fully hardcoded to iGive (Phase 22 concern to generalize if needed).
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes. `pageLabels` in BaseLayout.astro contains `/prosjekter/igive` → `'iGive'` and `/prosjekter/blom-company` → `'Blom Company'`. ProjectTeaser.astro `<a>` href is `/prosjekter/igive`.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npm run build` passes with no errors or warnings
2. `src/config/projects.ts` exports `Project` interface with `slug` field (confirms downstream plans can use it)
3. `projects` array contains entries for both `'igive'` and `'blom-company'`
4. `caseStudySection` does not appear anywhere in the file
5. `BaseLayout.astro` pageLabels contains entries for both case study paths
6. `ProjectTeaser.astro` href targets `/prosjekter/igive`
</verification>

<success_criteria>
- `Project` interface has all required new fields; `caseStudySection` removed
- Both project entries exist in `projects.ts` with valid data
- `npm run build` passes (no TypeScript errors, no missing imports)
- BreadcrumbList will render "iGive" and "Blom Company" (not raw slugs) when case study pages exist
- Homepage "Se prosjektet" link targets the iGive case study URL
</success_criteria>

<output>
After completion, create `.planning/phases/21-konfig-og-indeks/21-01-SUMMARY.md` following the summary template.
</output>
