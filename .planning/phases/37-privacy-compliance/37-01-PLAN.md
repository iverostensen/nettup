---
phase: 37-privacy-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/personvern/index.astro
  - .env.example
autonomous: true
requirements:
  - PRIV-01
  - PRIV-02

must_haves:
  truths:
    - "/personvern page discloses Meta Pixel usage including data processor, purpose, scope, and consent mechanism"
    - "Meta Pixel can be disabled by leaving PUBLIC_META_PIXEL_ID empty — no code changes required"
  artifacts:
    - path: "src/pages/personvern/index.astro"
      provides: "Meta Pixel disclosure across sections 2, 3, 4, 5, 6"
      contains: "Meta Pixel"
    - path: ".env.example"
      provides: "Kill switch documentation"
      contains: "PUBLIC_META_PIXEL_ID"
  key_links:
    - from: "src/pages/personvern/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "documents the pixel behavior implemented in BaseLayout"
      pattern: "alle sider"
---

<objective>
Update the privacy policy page with Meta Pixel disclosure and document the environment-variable kill switch — legal prerequisites before the pixel goes live.

Purpose: GDPR and Datatilsynet compliance requires transparent disclosure of all tracking technologies before activating ad campaigns.
Output: Updated /personvern page with Meta Pixel sections + .env.example with kill switch documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-privacy-compliance/37-CONTEXT.md
@.planning/phases/37-privacy-compliance/37-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Meta Pixel disclosure to personvern page (PRIV-01)</name>
  <files>src/pages/personvern/index.astro</files>
  <read_first>
    - src/pages/personvern/index.astro (full file — current state of all 10 sections)
    - src/layouts/BaseLayout.astro (verify pixel scope — loaded site-wide via BaseLayout)
  </read_first>
  <action>
Update `src/pages/personvern/index.astro` with Meta Pixel additions in 6 locations. All changes mirror existing Google Ads patterns exactly. Per D-07, update `lastUpdated` to `'28. mars 2026'`.

**1. Update lastUpdated (line 5) per D-07:**
Change `const lastUpdated = '19. mars 2026';` to `const lastUpdated = '28. mars 2026';`

**2. Add section 2.4 after the closing `</p>` of section 2.3 (after line 112, before `</section>` on line 113) per D-01, D-02:**

Insert this block:

```html
<h3 class="mb-3 mt-6 text-lg font-medium text-text">2.4 Meta Pixel (alle sider)</h3>
<p class="mb-3 text-text-muted">
  Vi bruker Meta Pixel (Facebook) på alle sider for å måle effekten av våre
  annonser på Facebook og Instagram. Meta Pixel lastes på alle sider via vår
  hovedmal, men hvordan den fungerer avhenger av ditt samtykke:
</p>
<div class="mt-4 space-y-4">
  <div class="rounded-lg bg-surface p-4">
    <h4 class="font-medium text-text">Hvis du godtar</h4>
    <p class="text-sm text-text-muted">
      Meta kan lagre informasjonskapsler (_fbp, _fbc) for å måle om du
      gjennomfører en handling etter å ha sett en annonse (konverteringssporing).
      Meta kan også bruke dataene til retargeting — altså å vise deg relevante
      annonser på Facebook og Instagram.
    </p>
  </div>
  <div class="rounded-lg bg-surface p-4">
    <h4 class="font-medium text-text">Hvis du avslår</h4>
    <p class="text-sm text-text-muted">
      Meta lagrer ingen informasjonskapsler og samler ikke inn personopplysninger.
      Pikselen laster ikke, og ingen data sendes til Meta.
    </p>
  </div>
</div>
<p class="mt-4 text-text-muted">
  Samtykke styres av samme valg som Google Ads — lagret i nettleserens lokale
  lagring under nøkkelen
  <code class="rounded bg-surface px-1 text-xs">nettup_ads_consent</code>.
</p>
```

**3. Add row to section 3 table (after the "Male annonseeffekt (Google Ads)" row, around line 139) per D-04:**

Insert after the Google Ads `</tr>`:
```html
<tr class="border-b border-white/5">
  <td class="py-3">Måle annonseeffekt (Meta Pixel)</td>
  <td class="py-3">Samtykke (GDPR art. 6(1)(a))</td>
</tr>
```

**4. Add Meta data processor entry in section 4 (after the Google entry, before the closing `</div>` of the `space-y-4` container, around line 193) per D-03:**

Insert after the Google `</div>`:
```html
<div class="rounded-lg bg-surface p-4">
  <h4 class="font-medium text-text">Meta (Facebook)</h4>
  <p class="text-sm text-text-muted">
    Meta Pixel for annonsekonvertering og retargeting på alle sider. Lagrer kun
    informasjonskapsler etter eksplisitt samtykke. Uten samtykke lastes ikke pikselen.
  </p>
  <p class="mt-1 text-xs text-text-muted">
    Lokasjon: USA (dekket av EU-US Data Privacy Framework)
  </p>
</div>
```

**5. Update section 5 to mention Meta Pixel cookies site-wide per D-05:**

In the "Hovedsiden" subsection (around line 206-208), replace the paragraph text. The current text says the main site sets no cookies — this is no longer accurate since Meta Pixel loads site-wide. Replace:

```html
<h3 class="mb-3 mt-4 text-lg font-medium text-text">Hovedsiden (nettup.no)</h3>
<p class="text-text-muted">
  Hovedsiden setter <strong class="text-text">ingen informasjonskapsler</strong> uten ditt samtykke.
  Vi bruker Plausible Analytics som er helt cookiefri. Dersom du godtar annonsecookies i
  samtykkebanneret, kan Meta Pixel sette informasjonskapsler (_fbp, _fbc) for
  konverteringssporing og retargeting.
</p>
```

In the "Landingssider" subsection, add a bullet for Meta after the Google bullets:

```html
<li><strong class="text-text">Meta Pixel:</strong> Lastes kun etter samtykke. Ved godkjenning settes _fbp og _fbc cookies for konverteringssporing</li>
```

**6. Add Meta Pixel row to section 6 table (after the Google Ads cookies row, around line 249) per D-06:**

Insert after the Google Ads `</tr>`:
```html
<tr class="border-b border-white/5">
  <td class="py-3">Meta Pixel cookies (_fbp, _fbc)</td>
  <td class="py-3">Opptil 90 dager (settes av Meta)</td>
</tr>
```

**Important:** Do NOT renumber sections 7-10. Do NOT change any existing section content beyond the specific additions above.
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && grep -c "Meta Pixel" src/pages/personvern/index.astro && grep "lastUpdated" src/pages/personvern/index.astro | head -1 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/personvern/index.astro contains "2.4 Meta Pixel (alle sider)" (section 2.4 heading)
    - src/pages/personvern/index.astro contains "Måle annonseeffekt (Meta Pixel)" (section 3 table row)
    - src/pages/personvern/index.astro contains "Meta (Facebook)" in a h4 element (section 4 data processor)
    - src/pages/personvern/index.astro contains "_fbp, _fbc" (cookie names in sections 2.4 and 5)
    - src/pages/personvern/index.astro contains "nettup_ads_consent" at least twice (section 2.3 original + section 2.4 addition)
    - src/pages/personvern/index.astro contains "Meta Pixel cookies (_fbp, _fbc)" (section 6 table row)
    - src/pages/personvern/index.astro contains "28. mars 2026" (updated date per D-07)
    - src/pages/personvern/index.astro does NOT contain "11." (no renumbered sections — max section is 10)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    /personvern page includes Meta Pixel disclosure in all 5 relevant sections (2.4, 3, 4, 5, 6), date updated, build passes. Decisions D-01 through D-07 satisfied.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document kill switch in .env.example (PRIV-02)</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (current state — has only ANTHROPIC_API_KEY)
    - src/layouts/BaseLayout.astro (verify the kill switch implementation: `const pixelId = import.meta.env.PUBLIC_META_PIXEL_ID || ''` and `if (pixelId)` guard)
  </read_first>
  <action>
Append to `.env.example` (do NOT remove the existing `ANTHROPIC_API_KEY` line). Add a blank line separator then:

```bash
# Meta Pixel ID — leave empty to disable Meta Pixel entirely (kill switch)
# Get your Pixel ID from Meta Events Manager: https://business.facebook.com/events_manager
PUBLIC_META_PIXEL_ID=
```

Per D-08: this documents the existing kill switch mechanism (empty value = pixel disabled via `if (pixelId)` guard in BaseLayout.astro and LandingPageLayout.astro). Per D-09: no separate PIXEL_ENABLED flag. Per D-10: no mention of kill switch on the privacy page.
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && grep "ANTHROPIC_API_KEY" .env.example && grep "PUBLIC_META_PIXEL_ID" .env.example && grep "kill switch" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - .env.example contains "ANTHROPIC_API_KEY" (existing entry preserved)
    - .env.example contains "PUBLIC_META_PIXEL_ID=" (new entry)
    - .env.example contains "kill switch" in a comment
    - .env.example does NOT contain "PIXEL_ENABLED" (per D-09, no separate boolean flag)
    - .env.example has exactly 2 non-comment, non-empty lines (ANTHROPIC_API_KEY and PUBLIC_META_PIXEL_ID)
  </acceptance_criteria>
  <done>
    .env.example documents the Meta Pixel kill switch. Operators know that leaving PUBLIC_META_PIXEL_ID empty disables the pixel entirely. Decisions D-08, D-09, D-10 satisfied.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "Meta Pixel" src/pages/personvern/index.astro` returns 5+ (appears in sections 2.4, 3, 4, 5, 6)
2. `grep "PUBLIC_META_PIXEL_ID" .env.example` shows the kill switch entry
3. `npm run build` exits 0 — no Astro compilation errors
4. Sections 7-10 in personvern page are unchanged (no renumbering)
</verification>

<success_criteria>
- /personvern page includes Meta Pixel disclosure covering data processor (Meta/Facebook), purpose (ad measurement + retargeting), scope (alle sider), and consent mechanism (nettup_ads_consent)
- .env.example documents PUBLIC_META_PIXEL_ID as the kill switch with clear comment
- Build passes cleanly
- All 10 locked decisions (D-01 through D-10) are satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/37-privacy-compliance/37-01-SUMMARY.md`
</output>
