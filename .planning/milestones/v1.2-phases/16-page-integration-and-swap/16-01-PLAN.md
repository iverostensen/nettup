---
phase: 16-page-integration-and-swap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/priskalkulator/index.astro
  - src/layouts/BaseLayout.astro
autonomous: true
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03

must_haves:
  truths:
    - "/priskalkulator loads with title, H1, calculator, and CTA section"
    - "Breadcrumbs on /priskalkulator show 'Hjem / Prisestimator'"
    - "SmartPrisKalkulator behaves identically on /priskalkulator and /tjenester"
    - "No files in src/components/islands/wizard/ are dead code"
    - "grep finds zero references to PrisKalkulatorIsland anywhere in src/"
  artifacts:
    - path: src/pages/priskalkulator/index.astro
      provides: "Dedicated calculator page with BaseLayout, hero, calculator, CTA"
      exports: []
    - path: src/layouts/BaseLayout.astro
      provides: "pageLabels record includes /priskalkulator entry"
      contains: "'/priskalkulator': 'Prisestimator'"
  key_links:
    - from: src/pages/priskalkulator/index.astro
      to: src/pages/tjenester/_sections/PrisKalkulator.astro
      via: "both import SmartPrisKalkulator — identical component, same behavior"
      pattern: "SmartPrisKalkulator"
    - from: src/layouts/BaseLayout.astro
      to: breadcrumb JSON-LD
      via: "pageLabels lookup at runtime"
      pattern: "pageLabels\\[fullPath\\]"
---

<objective>
Create the dedicated /priskalkulator page and wire it into BaseLayout breadcrumbs. Audit the wizard directory for dead code and confirm /tjenester embed is correct.

Purpose: Closes the v1.2 milestone — the new SmartPrisKalkulator is publicly accessible as a standalone page and cleanly embedded on /tjenester, with no dead code remaining.
Output: src/pages/priskalkulator/index.astro, updated BaseLayout.astro pageLabels, clean wizard directory.
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-page-integration-and-swap/16-CONTEXT.md

@src/layouts/BaseLayout.astro
@src/pages/tjenester/index.astro
@src/pages/tjenester/_sections/PrisKalkulator.astro
@src/components/sections/CTA.astro
@src/components/ui/Section.astro
@src/components/ui/SectionHeader.astro

<interfaces>
<!-- Key patterns for the executor. Extracted from codebase. -->

From src/layouts/BaseLayout.astro (line 24-33):
```astro
const pageLabels: Record<string, string> = {
  '/': 'Hjem',
  '/tjenester': 'Tjenester',
  '/tjenester/nettside': 'Nettside',
  '/tjenester/nettbutikk': 'Nettbutikk',
  '/tjenester/landingsside': 'Landingsside',
  '/om-oss': 'Om oss',
  '/prosjekter': 'Prosjekter',
  '/kontakt': 'Kontakt',
};
```
Add: `'/priskalkulator': 'Prisestimator'`

From src/pages/tjenester/_sections/PrisKalkulator.astro:
```astro
import SmartPrisKalkulator from '@/components/islands/SmartPrisKalkulator';
// ...
<SmartPrisKalkulator client:visible />
```
Use the same import and directive on the dedicated page.

From src/pages/om-oss/index.astro — page structure pattern:
```astro
<BaseLayout title="..." description="...">
  <main>
    <Hero />
    <Values />
    <OmOssCTA />
  </main>
</BaseLayout>
```

From src/components/ui/Section.astro — Section props:
- `background`: 'default' | 'raised'
- `padding`: 'sm' | 'md' | 'lg' | 'hero'
- `class`: extra classes (e.g. `pt-32 md:pt-40` for first section below nav)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /priskalkulator page and add pageLabels entry</name>
  <files>src/pages/priskalkulator/index.astro, src/layouts/BaseLayout.astro</files>
  <action>
Create `src/pages/priskalkulator/index.astro` as a standalone Astro page.

Page structure (inline — no separate section files needed, the page is simple):
- Use `BaseLayout` with:
  - `title="Finn prisen på din nettside | Nettup"`
  - `description="Få et raskt prisestimat for nettside, nettbutikk eller landingsside. Svar på noen spørsmål — vi viser deg hva det koster."`
- Inside `<main>`:
  1. **Hero section**: Use `Section` with `class="pt-32 md:pt-40"`. Inside: an `<h1>` with `font-display text-4xl font-bold md:text-5xl` and content "Få et prisestimat". Below the H1: a `<p class="mt-4 text-lg text-text-muted max-w-2xl">` with content "Svar på noen enkle spørsmål, så finner vi riktig løsning — og viser deg hva det koster. Ingen forpliktelser."
  2. **Calculator section**: Use `Section`. Inside: import and render `<SmartPrisKalkulator client:visible />` (same as PrisKalkulator.astro — do NOT wrap in SectionHeader, the page hero already sets context).
  3. **CTA section**: Import and render `<CTA />` from `@/components/sections/CTA.astro`.

Imports needed:
```astro
import BaseLayout from '@/layouts/BaseLayout.astro';
import Section from '@/components/ui/Section.astro';
import SmartPrisKalkulator from '@/components/islands/SmartPrisKalkulator';
import CTA from '@/components/sections/CTA.astro';
```

In `src/layouts/BaseLayout.astro`, add the `/priskalkulator` entry to the `pageLabels` record:
```ts
'/priskalkulator': 'Prisestimator',
```
Place it after the `/kontakt` entry. This enables the breadcrumb JSON-LD to resolve "Prisestimator" for the path.

Do NOT add /priskalkulator to FloatingNav — per user decision it is a tool page, not a top-level section.
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Build passes cleanly. File src/pages/priskalkulator/index.astro exists. BaseLayout.astro contains '/priskalkulator': 'Prisestimator' in pageLabels.</done>
</task>

<task type="auto">
  <name>Task 2: Audit wizard directory and verify no dead code or stale references</name>
  <files>src/components/islands/wizard/</files>
  <action>
Audit all files under `src/components/islands/wizard/` to confirm every file is imported and used.

Steps:
1. For each file in the directory, check that it is imported somewhere in the codebase:
   - `wizard-types.ts` — should be imported by wizard step files and/or SmartPrisKalkulator
   - `wizard-reducer.ts` — should be imported by SmartPrisKalkulator
   - `WizardStepper.tsx` — should be imported by SmartPrisKalkulator
   - `cards/SelectableCard.tsx` — should be imported by step components
   - `cards/GoalCard.tsx` — should be imported by GoalStep
   - `steps/GoalStep.tsx` through `steps/ResultStep.tsx` — should be imported by SmartPrisKalkulator

   Use grep to verify: `grep -r "wizard/" src/components/islands/SmartPrisKalkulator.tsx src/components/islands/wizard/`

2. Run: `grep -r "PrisKalkulatorIsland" src/` — must return zero matches.

3. If any wizard file is NOT imported anywhere: delete it. Do not archive, do not add TODO comments.

4. Verify /tjenester still embeds the calculator correctly: confirm `src/pages/tjenester/_sections/PrisKalkulator.astro` imports `SmartPrisKalkulator` and renders it with `client:visible`.

After all checks, run the build again to confirm nothing is broken.
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && grep -r "PrisKalkulatorIsland" src/ && echo "FOUND stale refs" || echo "Clean — no stale refs" && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Zero grep matches for PrisKalkulatorIsland. All files in src/components/islands/wizard/ are referenced. Build passes.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `npm run build` passes with 0 errors
2. `grep -r "PrisKalkulatorIsland" src/` returns no matches
3. `src/pages/priskalkulator/index.astro` exists and contains SmartPrisKalkulator
4. `src/layouts/BaseLayout.astro` pageLabels contains `/priskalkulator`
5. `src/pages/tjenester/_sections/PrisKalkulator.astro` is unchanged and still embeds SmartPrisKalkulator
</verification>

<success_criteria>
1. /priskalkulator exists as a dedicated Astro page with title "Finn prisen på din nettside | Nettup", H1 "Få et prisestimat", the SmartPrisKalkulator, and a CTA section
2. Breadcrumb JSON-LD resolves /priskalkulator as "Prisestimator" via pageLabels
3. /tjenester continues to embed SmartPrisKalkulator identically — no changes to its embed
4. grep confirms zero PrisKalkulatorIsland references in src/
5. All wizard directory files are actively used — no dead code
6. Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/16-page-integration-and-swap/16-01-SUMMARY.md`
</output>
