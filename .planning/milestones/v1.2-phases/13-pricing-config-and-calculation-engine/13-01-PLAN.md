---
phase: 13-pricing-config-and-calculation-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/pricing-config.ts
autonomous: true
requirements:
  - PRIS-01

must_haves:
  truths:
    - "A single TypeScript config file defines base prices, add-on options with prices, and monthly costs for all 3 services"
    - "Adding or changing a price requires editing only the config file"
    - "Config categories (sizes, features, integrations, designs) are iterable arrays with id+label for UI rendering"
  artifacts:
    - path: "src/config/pricing-config.ts"
      provides: "Complete pricing data model with types and config data for nettside, nettbutikk, landingsside"
      exports: ["ServiceType", "SizeTier", "AddOn", "DesignLevel", "ServicePricing", "DiscountConfig", "PricingConfig", "EstimateRequest", "EstimateResult", "LineItem", "pricingConfig"]
  key_links:
    - from: "src/config/pricing-config.ts"
      to: "src/config/services.ts"
      via: "ServiceType slugs match services.ts slugs"
      pattern: "nettside.*nettbutikk.*landingsside"
---

<objective>
Create the typed pricing config file with all service pricing data and engine type definitions.

Purpose: Establish the single source of truth for all pricing data and the type contracts that the calculation engine (Plan 02) and downstream phases (14-16) will consume.
Output: `src/config/pricing-config.ts` with exported types, interfaces, and config data.
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pricing-config-and-calculation-engine/13-CONTEXT.md
@.planning/phases/13-pricing-config-and-calculation-engine/13-RESEARCH.md
@src/config/services.ts
@src/config/pricing.ts

<interfaces>
<!-- Existing services.ts slugs that ServiceType must match -->
From src/config/services.ts:
```typescript
// slugs: 'nettside', 'nettbutikk', 'landingsside'
// monthlyPrice: 350 (nettside), 500 (nettbutikk), 250 (landingsside)
export const LAUNCH_DISCOUNT = 0.4;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define types and interfaces for pricing system</name>
  <files>src/config/pricing-config.ts</files>
  <action>
Create `src/config/pricing-config.ts` with two sections: type definitions and config data.

**Types to export:**

1. `ServiceType` = `'nettside' | 'nettbutikk' | 'landingsside'` -- must match services.ts slugs exactly
2. `SizeTier` = `{ id: string; label: string; minPrice: number; maxPrice: number }` -- single-select, provides min-max range
3. `AddOn` = `{ id: string; label: string; price: number }` -- fixed one-time price in NOK
4. `DesignLevel` = `{ id: string; label: string; price: number }` -- additional cost on top of base
5. `ServicePricing` = `{ sizes: SizeTier[]; features: AddOn[]; integrations: AddOn[]; designs: DesignLevel[]; monthlyPrice: number }`
6. `DiscountConfig` = `{ percentage: number; label: string; active: boolean }`
7. `PricingConfig` = `{ discount: DiscountConfig; services: Record<ServiceType, ServicePricing> }`
8. `EstimateRequest` = `{ serviceType: ServiceType; sizeId: string; featureIds: string[]; integrationIds: string[]; designId: string }` -- engine input
9. `LineItem` = `{ category: 'size' | 'feature' | 'integration' | 'design'; id: string; label: string; price: number }` -- engine output line
10. `EstimateResult` = `{ serviceType: ServiceType; lineItems: LineItem[]; oneTime: { min: number; max: number }; discounted: { min: number; max: number }; monthly: number; discountPercent: number; discountActive: boolean }` -- engine output

**Config data (`pricingConfig`):**

Discount: `{ percentage: 0.4, label: 'Lanseringstilbud', active: true }`

For each service, populate with realistic Norwegian SMB web agency pricing (Claude's discretion on exact amounts). Guidelines:
- **nettside**: sizes Small (1-5 sider, ~8k-12k), Medium (6-15 sider, ~15k-25k), Large (16+, ~28k-45k). Features: CMS, kontaktskjema, SEO-optimalisering, animasjoner, analytics-oppsett, blogg, flerspraklig, booking-system, nyhetsbrev, brukerpanel/innlogging. Integrations: fewer (e.g., Google Analytics, CRM). Designs: Standard (0), Skreddersydd (~5k), Premium (~10k). monthlyPrice: 350.
- **nettbutikk**: sizes reflect higher base (commerce complexity). Features: subset relevant to e-commerce. Integrations: Vipps, Klarna, regnskap/ERP, frakt-integrasjon, lagerstyring. monthlyPrice: 500.
- **landingsside**: sizes reflect simpler scope (1 side, 2-3 sider, 4+ sider). Fewer features (no CMS, no blogg). Minimal integrations. monthlyPrice: 250.

Do NOT import from or modify services.ts. Do NOT modify pricing.ts. The new config is additive only.

Use `@/*` path alias convention. All labels in Norwegian.
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && npx tsc --noEmit src/config/pricing-config.ts 2>&1 | head -20</automated>
  </verify>
  <done>
- pricing-config.ts exists with all 10 exported types/interfaces
- pricingConfig constant exported with data for all 3 services
- Each service has sizes (array), features (array), integrations (array), designs (3 levels), monthlyPrice
- Discount config has percentage=0.4, label, active=true
- ServiceType matches services.ts slugs: nettside, nettbutikk, landingsside
- File compiles with zero TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `pricing-config.ts` exports all required types and the config constant
- Config covers all 3 service types with populated categories
- No modifications to existing files (services.ts, pricing.ts)
</verification>

<success_criteria>
A typed pricing config file exists that any downstream consumer can import for both type information and pricing data. All prices are in NOK, all labels in Norwegian, and the structure supports both UI iteration (arrays with id+label) and engine calculation (prices on every item).
</success_criteria>

<output>
After completion, create `.planning/phases/13-pricing-config-and-calculation-engine/13-01-SUMMARY.md`
</output>
