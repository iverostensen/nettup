---
phase: 04-conversion-optimization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/om-oss/_sections/OmOssCTA.astro
  - src/pages/prosjekter/_sections/ProsjekterCTA.astro
  - src/pages/om-oss/index.astro
  - src/pages/prosjekter/index.astro
  - src/pages/tjenester/_sections/Pakker.astro
  - src/pages/tjenester/_sections/TjenesterCTA.astro
autonomous: true
requirements:
  - CONV-01
  - CONV-02

must_haves:
  truths:
    - "Visitor on /om-oss sees a CTA that references Nettup's approach/results — not the generic 'Klar for en nettside som funker?'"
    - "Visitor on /prosjekter sees a CTA inviting them to become the next success story — specific to portfolio context"
    - "Clicking 'Kom i gang' on a pricing package navigates to /kontakt with ?pakke=enkel/standard/premium in the URL"
    - "The contact form pre-selects and shows a badge for the correct package when arrived via ?pakke= URL"
    - "TjenesterCTA bottom button includes ?kilde=tjenester for tracking"
  artifacts:
    - path: "src/pages/om-oss/_sections/OmOssCTA.astro"
      provides: "Contextual CTA for /om-oss"
      contains: "Vil du ha en nettside som gir resultater"
    - path: "src/pages/prosjekter/_sections/ProsjekterCTA.astro"
      provides: "Contextual CTA for /prosjekter"
      contains: "Din bedrift kan bli neste suksesshistorie"
    - path: "src/pages/tjenester/_sections/Pakker.astro"
      provides: "Pricing cards with pre-fill hrefs"
      contains: "?pakke=${pakke.id}&kilde=tjenester"
  key_links:
    - from: "src/pages/om-oss/index.astro"
      to: "src/pages/om-oss/_sections/OmOssCTA.astro"
      via: "import OmOssCTA + replace CTA component"
      pattern: "OmOssCTA"
    - from: "src/pages/prosjekter/index.astro"
      to: "src/pages/prosjekter/_sections/ProsjekterCTA.astro"
      via: "import ProsjekterCTA + replace CTA component"
      pattern: "ProsjekterCTA"
    - from: "src/pages/tjenester/_sections/Pakker.astro"
      to: "/kontakt"
      via: "Button href template literal with ?pakke= param"
      pattern: "pakke\\.id.*kilde=tjenester"
---

<objective>
Make every page's bottom CTA feel like a natural continuation of what the visitor just read, and wire the pricing package buttons to pre-fill the contact form.

Purpose: Reduce friction from interest to inquiry. Visitors who just read about Nettup's approach or portfolio results should encounter a CTA that acknowledges what they read and invites them to take the obvious next step. Package button pre-fill removes the "which service did I want?" moment when landing on the contact form.
Output: Two new page-specific CTA sections, two updated index files, one Pakker.astro href update, one TjenesterCTA tracking param.
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-conversion-optimization/04-CONTEXT.md
@.planning/phases/04-conversion-optimization/04-RESEARCH.md

<interfaces>
<!-- Key patterns the executor needs. Extracted from codebase. -->
<!-- Use these directly — no codebase exploration needed. -->

Reference pattern — src/pages/tjenester/_sections/TjenesterCTA.astro (existing page-specific CTA):
```astro
---
import Section from '@/components/ui/Section.astro';
import Button from '@/components/ui/Button.astro';
---
<Section background="raised">
  <div class="reveal-on-scroll mx-auto max-w-2xl text-center">
    <h2 class="text-3xl font-bold md:text-4xl">
      Usikker på hvilken pakke?
    </h2>
    <p class="mt-4 text-lg text-text-muted">
      Ta kontakt så hjelper vi deg å finne riktig løsning for din bedrift.
    </p>
    <div class="mt-8">
      <Button href="/kontakt" size="lg">Ta en uforpliktende prat</Button>
    </div>
  </div>
</Section>
```

src/pages/om-oss/index.astro (current — CTA import to replace):
```astro
import Hero from './_sections/Hero.astro';
import Values from './_sections/Values.astro';
import Approach from './_sections/Approach.astro';
import CTA from '@/components/sections/CTA.astro';
// ...
<Hero />
<Values />
<Approach />
<CTA />
```

src/pages/prosjekter/index.astro (current — CTA import to replace):
```astro
import Hero from './_sections/Hero.astro';
import ProjectShowcase from './_sections/ProjectShowcase.astro';
import Results from './_sections/Results.astro';
import CTA from '@/components/sections/CTA.astro';
// ...
<Hero />
<ProjectShowcase />
<Results />
<CTA />
```

Pakker.astro button (current — needs href change):
```astro
<Button
  href="/kontakt"
  variant={pakke.popular ? 'primary' : 'secondary'}
  class="w-full"
>
  Kom i gang
</Button>

<!-- After (pakke.id values: 'enkel' | 'standard' | 'premium') -->
<Button
  href={`/kontakt?pakke=${pakke.id}&kilde=tjenester`}
  variant={pakke.popular ? 'primary' : 'secondary'}
  class="w-full"
>
  Kom i gang
</Button>
```

TjenesterCTA.astro button (current):
```astro
<Button href="/kontakt" size="lg">Ta en uforpliktende prat</Button>
<!-- After (add kilde tracking): -->
<Button href="/kontakt?kilde=tjenester" size="lg">Ta en uforpliktende prat</Button>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create contextual CTAs for /om-oss and /prosjekter</name>
  <files>
    src/pages/om-oss/_sections/OmOssCTA.astro,
    src/pages/prosjekter/_sections/ProsjekterCTA.astro,
    src/pages/om-oss/index.astro,
    src/pages/prosjekter/index.astro
  </files>
  <action>
    Create two page-specific CTA files following the exact `TjenesterCTA.astro` structure (Section > div.reveal-on-scroll > h2, p, Button). Do NOT add inline or mid-page CTAs — one bottom CTA per page only.

    **OmOssCTA.astro** — follows /om-oss Approach section (team values, 2-week delivery, "vi bygger det som faktisk hjelper"):
    - h2: "Vil du ha en nettside som gir resultater?"
    - p: "Vi bygger det som faktisk hjelper bedriften din — rask, tydelig og mobilvennlig. La oss snakke om hva du trenger."
    - Button href="/kontakt" size="lg": "Start samtalen"

    **ProsjekterCTA.astro** — follows /prosjekter Results section (Lighthouse scores, delivery proof, iGive case):
    - h2: "Din bedrift kan bli neste suksesshistorie"
    - p: "Vi har levert raske, moderne nettsider for norske bedrifter. La oss gjøre det samme for deg."
    - Button href="/kontakt" size="lg": "Få ditt tilbud"

    Then update the two index files:
    - `src/pages/om-oss/index.astro`: Replace `import CTA from '@/components/sections/CTA.astro'` with `import OmOssCTA from './_sections/OmOssCTA.astro'`. Replace `<CTA />` with `<OmOssCTA />`.
    - `src/pages/prosjekter/index.astro`: Replace `import CTA from '@/components/sections/CTA.astro'` with `import ProsjekterCTA from './_sections/ProsjekterCTA.astro'`. Replace `<CTA />` with `<ProsjekterCTA />`.

    The homepage (`/`) keeps the shared `CTA.astro` — it already has adequate copy after the Testimonials section.
  </action>
  <verify>
    `npm run build` exits 0. Check that `/om-oss` and `/prosjekter` pages build without error. Manual spot-check: grep for "OmOssCTA" in om-oss/index.astro and "ProsjekterCTA" in prosjekter/index.astro.
  </verify>
  <done>
    OmOssCTA.astro and ProsjekterCTA.astro exist with contextual Norwegian copy. Both index.astro files import and render the page-specific CTA. `npm run build` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire pricing package pre-fill and TjenesterCTA tracking</name>
  <files>
    src/pages/tjenester/_sections/Pakker.astro,
    src/pages/tjenester/_sections/TjenesterCTA.astro
  </files>
  <action>
    In `src/pages/tjenester/_sections/Pakker.astro`, find the Button inside the `pakker.map()` loop (line ~63-69) that has `href="/kontakt"`. Change it to use a template literal:

    ```astro
    href={`/kontakt?pakke=${pakke.id}&kilde=tjenester`}
    ```

    CRITICAL: Use a JSX expression with backtick template literal — NOT string interpolation like `href="/kontakt?pakke={pakke.id}"` (that renders literally). The `pakke.id` values from `src/config/pricing.ts` are `'enkel'`, `'standard'`, `'premium'` — these match exactly what `ContactForm.tsx` validates in its `useEffect`.

    In `src/pages/tjenester/_sections/TjenesterCTA.astro`, update the Button href from `"/kontakt"` to `"/kontakt?kilde=tjenester"` for tracking purposes.

    No other changes to these files.
  </action>
  <verify>
    `npm run build` exits 0. In browser (npm run dev): click "Kom i gang" on the Standard package → URL should be `/kontakt?pakke=standard&kilde=tjenester`. Confirm the confirmation badge appears in the contact form. Also verify Enkel and Premium package buttons produce their respective ?pakke= values.
  </verify>
  <done>
    All three "Kom i gang" buttons in Pakker.astro link to `/kontakt?pakke={enkel|standard|premium}&kilde=tjenester`. TjenesterCTA button links to `/kontakt?kilde=tjenester`. `npm run build` passes.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `npm run build` passes with 0 errors
2. `/om-oss` page: bottom CTA says "Vil du ha en nettside som gir resultater?" — NOT the generic "Klar for en nettside som funker?"
3. `/prosjekter` page: bottom CTA says "Din bedrift kan bli neste suksesshistorie"
4. `/tjenester` pricing cards: each "Kom i gang" button href contains `?pakke=` and `?kilde=tjenester`
5. Navigating to `/kontakt?pakke=standard` shows a pre-selection badge in the contact form
6. Homepage CTA remains unchanged (still uses shared CTA.astro)
</verification>

<success_criteria>
- CONV-01: /om-oss and /prosjekter have contextually relevant bottom CTAs with copy derived from page content
- CONV-02: All three pricing package buttons navigate to /kontakt with ?pakke=X pre-fill; contact form shows confirmation badge
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/04-conversion-optimization/04-01-SUMMARY.md` following the summary template.
</output>
