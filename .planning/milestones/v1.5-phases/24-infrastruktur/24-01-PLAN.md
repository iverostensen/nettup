---
phase: 24-infrastruktur
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/locations.ts
  - src/pages/steder/[location].astro
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04

must_haves:
  truths:
    - "`npm run build` passerer uten feil med to stub-entries (Oslo og Bærum)"
    - "URL `/steder/oslo` og `/steder/baerum` genereres som statiske sider"
    - "Slug-konvensjon (`æ→ae`, `ø→o`, `å→a`) er dokumentert i `locations.ts` som kommentar"
    - "Bærum-siden viser slug `baerum` (bekrefter `æ→ae`-konvertering)"
    - "Canonical URL er korrekt self-refererende på begge sider (via BaseLayout)"
    - "Ingen `noindex` meta-tag på bysider"
  artifacts:
    - path: src/config/locations.ts
      provides: "City interface, ACTIVE_TIER konstant, cities[] array med Oslo og Bærum"
      exports: [City, ACTIVE_TIER, cities]
    - path: src/pages/steder/[location].astro
      provides: "Dynamisk rute med getStaticPaths(), komplett sidelayout"
      contains: "getStaticPaths, city.tier <= ACTIVE_TIER filter"
  key_links:
    - from: src/pages/steder/[location].astro
      to: src/config/locations.ts
      via: "import { cities, ACTIVE_TIER, type City }"
      pattern: "cities.*filter.*ACTIVE_TIER"
    - from: src/pages/steder/[location].astro
      to: src/layouts/BaseLayout.astro
      via: "BaseLayout title={city.metaTitle} description={city.metaDescription}"
      pattern: "BaseLayout.*metaTitle"
---

<objective>
Lock the TypeScript infrastructure for local SEO city pages: the `locations.ts` config (City interface, ACTIVE_TIER constant, two stub entries) and the `steder/[location].astro` dynamic route with full section layout. Build must pass with both stub entries.

Purpose: Establishes the data model and routing skeleton that Phase 25 (city content) and Phase 26 (SEO/linking) depend on. Getting the interface right now prevents structural changes when 6–8 cities are added in the next phase.

Output: Two new files. No new dependencies. All patterns mirror existing `projects.ts` → `prosjekter/[slug].astro`.
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-infrastruktur/24-CONTEXT.md
@.planning/phases/24-infrastruktur/24-RESEARCH.md

<interfaces>
<!-- Existing patterns the executor should mirror — extracted from codebase. No exploration needed. -->

From src/config/projects.ts (interface pattern to mirror):
```typescript
export interface Project {
  slug: string;
  name: string;
  metaTitle: string;
  metaDescription: string;
  // optional fields use ?:
  url?: string;
}
export const projects: Project[] = [ ... ];
```

From src/pages/prosjekter/[slug].astro (getStaticPaths pattern to mirror exactly):
```typescript
import { projects } from '@/config/projects';
export function getStaticPaths() {
  return projects.map((project) => ({
    params: { slug: project.slug },
    props: { project },
  }));
}
interface Props { project: Project; }
const { project } = Astro.props;
```

From src/layouts/BaseLayout.astro (canonical is automatic):
```typescript
// Line 22 — canonical computed automatically from Astro.url.pathname
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
// Usage: <BaseLayout title="..." description="..."> — no extra canonical work needed
```

From src/components/ui/Breadcrumbs.astro (confirmed interface):
```astro
<Breadcrumbs items={[
  { label: 'Hjem', href: '/' },
  { label: 'Steder', href: '/steder' },
  { label: city.name },  // no href = current page
]} />
```

From src/pages/prosjekter/[slug].astro (Section usage):
```astro
<Section>                        <!-- default bg -->
<Section background="raised">    <!-- bg-surface-raised -->
```

From src/pages/prosjekter/[slug].astro (reveal-on-scroll pattern):
```astro
<h1 class="reveal-on-scroll reveal-stagger-1 text-4xl font-bold md:text-5xl">
<p class="reveal-on-scroll reveal-stagger-2 mt-4 text-text-muted">
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Opprett `locations.ts` med City-interface og to stub-entries</name>
  <files>src/config/locations.ts</files>
  <action>
Opprett `src/config/locations.ts` med følgende struktur, i denne rekkefølgen:

1. Slug-konvensjon som kommentarblokk øverst (INFRA-01):
   ```
   // Slug-konvensjon for norske bynavn:
   //   æ → ae  |  ø → o  |  å → a  |  mellomrom → bindestrek
   // Eksempler: Bærum → baerum, Lillestrøm → lillestrom, Ås → as
   // Bruk alltid ASCII-slugs i slug-feltet. Visningsnavn med norske tegn i name-feltet.
   ```

2. `export const ACTIVE_TIER = 1;` — eksportert konstant øverst, gjør tier-promotering til én linjeendring.

3. `export interface City` med nøyaktig disse feltene (INFRA-02):
   - `tier: 1 | 2 | 3` — talltype for range-filtrering (`city.tier <= ACTIVE_TIER`)
   - `slug: string` — ASCII-only, se konvensjon over
   - `name: string` — visningsnavn med norske tegn
   - `intro: string` — åpningsavsnitt for Hero-seksjonen
   - `faq: Array<{ question: string; answer: string }>` — by-spesifikke spørsmål
   - `nearbyAreas: string[]` — slugs til nabobyer for intern lenking
   - `metaTitle: string` — unik `<title>` per bysite
   - `metaDescription: string` — unik meta description
   - `industries?: string[]` — valgfri, varierer per by

4. `export const cities: City[] = [...]` med to stub-entries:

   **Oslo** (enkel slug, ingen spesialtegn):
   - `tier: 1, slug: 'oslo', name: 'Oslo'`
   - `intro`: 2–3 setninger om Nettup og Oslo — kortfattet men realistisk placeholder. Skriv på norsk (bokmål). Nevn at Nettup leverer profesjonelle nettsider til bedrifter i Oslo og nærliggende områder.
   - `faq`: 2 entries — eks. "Holder dere til i Oslo?" og "Hvor lang tid tar det å få en nettside?"
   - `nearbyAreas: ['baerum']` — bare Bærum finnes i Phase 24
   - `metaTitle: 'Nettside for bedrift i Oslo | Nettup'`
   - `metaDescription`: ~150 tegn, inkluderer "Oslo", "nettside", "bedrift"

   **Bærum** (tester `æ→ae`-konvertering):
   - `tier: 1, slug: 'baerum', name: 'Bærum'`
   - `intro`: 2–3 setninger om Nettup og Bærum. Norsk bokmål.
   - `faq`: 2 entries — eks. "Holder dere til i Bærum?" og "Hva koster en bedriftsnettside?"
   - `nearbyAreas: ['oslo']` — bare Oslo finnes i Phase 24
   - `metaTitle: 'Nettside for bedrift i Bærum | Nettup'`
   - `metaDescription`: ~150 tegn, inkluderer "Bærum", "nettside", "bedrift"

TypeScript strict mode: Alle påkrevde felter må være satt. Ingen `any`. Filen skal typeschecke uten feil.
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
`src/config/locations.ts` eksisterer og eksporterer `City`, `ACTIVE_TIER` og `cities`. Slug-konvensjonen er dokumentert som kommentar. Oslo-entry har slug `oslo`, Bærum-entry har slug `baerum`. TypeScript kompilerer uten feil.
  </done>
</task>

<task type="auto">
  <name>Task 2: Opprett `steder/[location].astro` med getStaticPaths() og komplett sidelayout</name>
  <files>src/pages/steder/[location].astro</files>
  <action>
Opprett `src/pages/steder/[location].astro`. Opprett mappen `src/pages/steder/` om den ikke finnes.

**Imports (øverst i frontmatter):**
```typescript
import { cities, ACTIVE_TIER, type City } from '@/config/locations';
import BaseLayout from '@/layouts/BaseLayout.astro';
import Breadcrumbs from '@/components/ui/Breadcrumbs.astro';
import Section from '@/components/ui/Section.astro';
import SectionHeader from '@/components/ui/SectionHeader.astro';
```

**getStaticPaths() — speil prosjekter/[slug].astro nøyaktig, men med tier-filter (INFRA-03):**
```typescript
export function getStaticPaths() {
  return cities
    .filter((city) => city.tier <= ACTIVE_TIER)
    .map((city) => ({
      params: { location: city.slug },
      props: { city },
    }));
}

interface Props {
  city: City;
}

const { city } = Astro.props;
```

**BaseLayout (INFRA-04 — canonical er automatisk via BaseLayout):**
```astro
<BaseLayout title={city.metaTitle} description={city.metaDescription}>
```
Ingen `<Fragment slot="head">` JSON-LD i Phase 24 — det er Phase 25-arbeid.

**Seksjonsrekkefølge (fra låste beslutninger i CONTEXT.md):**

1. **Breadcrumbs** — rett under `<main>`, før første Section:
   ```astro
   <div class="container pt-8">
     <Breadcrumbs items={[
       { label: 'Hjem', href: '/' },
       { label: 'Steder', href: '/steder' },
       { label: city.name },
     ]} />
   </div>
   ```

2. **Hero-seksjon** — `<Section>` med H1 og intro:
   ```astro
   <Section>
     <div class="max-w-3xl">
       <h1 class="reveal-on-scroll reveal-stagger-1 text-4xl font-bold md:text-5xl">
         Nettside for bedrift i {city.name}
       </h1>
       <p class="reveal-on-scroll reveal-stagger-2 mt-4 text-lg text-text-muted">
         {city.intro}
       </p>
     </div>
   </Section>
   ```

3. **FAQ-seksjon** — `<Section background="raised">` med `SectionHeader` og FAQ-items:
   - Overskrift: "Vanlige spørsmål" med by-navn i undertittel eller H2
   - Map over `city.faq` og render hvert spørsmål/svar. Bruk enkel `<details>`/`<summary>` eller divs — velg det som gir tydeligst visuell feedback i Phase 24. Ingen animert accordion — hold det enkelt.

4. **Nabobyer-seksjon** — `<Section>` med intern lenking:
   - Overskrift: "Områder vi dekker i nærheten"
   - Map over `city.nearbyAreas` og render lenker til `/steder/{slug}` for hvert element. Bruk tag-stil (pill-knapper) som lenker — konsistent med sitedesign.
   - Eksempel for ett element: `<a href={`/steder/${area}`} class="...">Bynavnet</a>`
   - Merk: `nearbyAreas` inneholder slugs — vis enten sluggen direkte, eller finn city.name fra cities-arrayet for penere visning. Finn by-navn fra `cities.find(c => c.slug === area)?.name ?? area`.

5. **CTA-seksjon** — `<Section>` med enkel CTA:
   - Heading: "Klar for en ny nettside?"
   - Knapp/lenke til `/kontakt`. Bruk eksisterende CTA-mønster — se `src/components/sections/` for gjenbrukbare CTA-seksjoner, eller bygg enkel inline CTA som matcher sidens stil.

**Build-verifisering:** Siden skal generere to statiske sider (`steder/oslo` og `steder/baerum`) og `npm run build` skal passere uten feil.
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
`src/pages/steder/[location].astro` eksisterer. `npm run build` passerer uten feil. Build-output inneholder `/steder/oslo/index.html` og `/steder/baerum/index.html`. Alle fem seksjoner er til stede (Breadcrumbs, Hero, FAQ, Nabobyer, CTA). Ingen TypeScript-feil.
  </done>
</task>

</tasks>

<verification>
Etter at begge tasker er fullført:

```bash
# TypeScript-kompilering
cd /Users/iverostensen/nettup && npx tsc --noEmit

# Full build
cd /Users/iverostensen/nettup && npm run build

# Bekreft at bysider er generert
ls dist/steder/oslo/ && ls dist/steder/baerum/

# Bekreft slug-konvensjon i output
grep -l "baerum" dist/steder/baerum/index.html

# Bekreft canonical-tag (BaseLayout setter denne automatisk)
grep "canonical" dist/steder/oslo/index.html

# Bekreft ingen noindex
grep -c "noindex" dist/steder/oslo/index.html  # skal returnere 0
```
</verification>

<success_criteria>
- `npm run build` passerer uten feil eller advarsler
- `dist/steder/oslo/index.html` og `dist/steder/baerum/index.html` eksisterer
- `src/config/locations.ts` eksporterer `City`, `ACTIVE_TIER = 1` og `cities[]` med Oslo og Bærum
- Slug-konvensjon er dokumentert som kommentar i `locations.ts` (INFRA-01)
- `City`-interface har alle påkrevde felter inkl. `tier: 1 | 2 | 3` og valgfri `industries?` (INFRA-02)
- `getStaticPaths()` filtrerer på `city.tier <= ACTIVE_TIER` (INFRA-03)
- Canonical-tag er til stede og self-refererende på begge sider (INFRA-04, via BaseLayout)
- Ingen `noindex` på bysider
</success_criteria>

<output>
After completion, create `.planning/phases/24-infrastruktur/24-01-SUMMARY.md` following the summary template.
</output>
