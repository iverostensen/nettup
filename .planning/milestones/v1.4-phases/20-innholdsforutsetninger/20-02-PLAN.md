---
phase: 20-innholdsforutsetninger
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - .planning/VISUAL-CONTENT-PLAN.md
autonomous: false
requirements:
  - INNHOLD-02

must_haves:
  truths:
    - "Lighthouse scores for salg.igive.no are recorded in VISUAL-CONTENT-PLAN.md — real measured numbers, not dashes or fabricated values"
    - "Lighthouse scores for blomcompany.com (or blom-no.vercel.app if DNS fails) are recorded — all 4 categories present"
    - "The Measured column in the Lighthouse table shows the date measured, not 'pending'"
  artifacts:
    - path: ".planning/VISUAL-CONTENT-PLAN.md"
      provides: "Filled Lighthouse scores table — source of truth for phase 22 metrics block"
      contains: "Real Performance, Accessibility, Best Practices, SEO values for both projects"
  key_links:
    - from: ".planning/VISUAL-CONTENT-PLAN.md Lighthouse table"
      to: "src/pages/prosjekter/igive/_sections/Metrics.astro (Phase 22)"
      via: "phase 22 reads scores from this document to populate visual metrics block"
      pattern: "Performance.*Accessibility.*Best Practices.*SEO"
---

<objective>
Measure Lighthouse/PageSpeed scores for both client projects and record real numbers in .planning/VISUAL-CONTENT-PLAN.md.

Purpose: Phase 22 must display concrete verifiable numbers in a visual metrics block — not the hardcoded "95" scores currently in Results.astro. This plan captures the real data.
Output: VISUAL-CONTENT-PLAN.md Lighthouse table filled with actual scores for salg.igive.no and blomcompany.com.
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/VISUAL-CONTENT-PLAN.md
@.planning/phases/20-innholdsforutsetninger/20-CONTEXT.md
@.planning/phases/20-innholdsforutsetninger/20-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Measure PageSpeed scores for both projects</name>
  <files>.planning/VISUAL-CONTENT-PLAN.md</files>
  <action>
    Human opens pagespeed.web.dev, runs analysis for salg.igive.no and blomcompany.com, and fills the Lighthouse scores table in VISUAL-CONTENT-PLAN.md with real numbers. If blomcompany.com DNS fails, use blom-no.vercel.app as fallback and update the URL column.
  </action>
  <what-claude-prepared>
    VISUAL-CONTENT-PLAN.md exists with the Lighthouse scores table ready to be filled in. The table has columns: Project | URL | Performance | Accessibility | Best Practices | SEO | Measured.
  </what-claude-prepared>
  <how-to-verify>
    1. Open https://pagespeed.web.dev in your browser
    2. Run analysis for **salg.igive.no** — wait for full results
    3. Record all 4 scores: Performance, Accessibility, Best Practices, SEO
    4. Run analysis for **blomcompany.com** — if DNS fails (ERR_NAME_NOT_RESOLVED or parking page), use **blom-no.vercel.app** instead
    5. Record all 4 scores for Blom Company
    6. Open .planning/VISUAL-CONTENT-PLAN.md and fill in the Lighthouse table:
       - Replace `—` with the actual scores
       - Replace `pending` in Measured column with today's date (YYYY-MM-DD)
       - If you used blom-no.vercel.app instead of blomcompany.com, update the URL column for that row
    7. Save the file
  </how-to-verify>
  <resume-signal>Type "scores recorded" once you have filled in all 8 score values (4 per project) in VISUAL-CONTENT-PLAN.md</resume-signal>
  <verify>
    <automated>grep -E "iGive|Blom" /Users/iverostensen/nettup/.planning/VISUAL-CONTENT-PLAN.md | grep -v " — " | grep -v "pending" | wc -l | xargs test 2 -le && echo "OK — scores recorded" || echo "FAIL — scores still pending"</automated>
  </verify>
  <done>Both rows in Lighthouse table have numeric scores (not dashes) and a date in the Measured column.</done>
</task>

</tasks>

<verification>
After checkpoint completes:
- grep -E "iGive|Blom" .planning/VISUAL-CONTENT-PLAN.md — rows should show numeric values, not "—"
- Both rows have a date in Measured column (not "pending")
</verification>

<success_criteria>
INNHOLD-02 satisfied: iGive and Blom Company Lighthouse scores (Performance, Accessibility, Best Practices, SEO) are recorded as real numbers in .planning/VISUAL-CONTENT-PLAN.md.
</success_criteria>

<output>
After completion, create `.planning/phases/20-innholdsforutsetninger/20-02-SUMMARY.md`
</output>
