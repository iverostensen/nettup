---
phase: 20-innholdsforutsetninger
plan: 03
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/assets/images/igive-hero.png
  - src/assets/images/igive-features.png
  - src/assets/images/blom-hero.png
  - src/assets/images/blom-features.png
  - src/config/projects.ts
  - src/pages/_home/ProjectTeaser.astro
  - src/pages/nettside-for-bedrift/_sections/VisualProof.astro
autonomous: false
requirements:
  - INNHOLD-03

must_haves:
  truths:
    - "igive-hero.png, igive-features.png, blom-hero.png, blom-features.png all exist in src/assets/images/"
    - "salg.igive.no.png no longer exists in src/assets/images/ (renamed to igive-hero.png)"
    - "No file in src/ imports salg.igive.no.png (all three import sites updated)"
    - "npm run build passes with no ENOENT errors"
    - "Status column in VISUAL-CONTENT-PLAN.md shows [x] for all four entries"
  artifacts:
    - path: "src/assets/images/igive-hero.png"
      provides: "iGive hero screenshot (1600x900)"
    - path: "src/assets/images/igive-features.png"
      provides: "iGive features section screenshot"
    - path: "src/assets/images/blom-hero.png"
      provides: "Blom Company hero screenshot (1600x900)"
    - path: "src/assets/images/blom-features.png"
      provides: "Blom Company features/product section screenshot"
    - path: "src/config/projects.ts"
      provides: "Updated import path: igive-hero.png"
    - path: "src/pages/_home/ProjectTeaser.astro"
      provides: "Updated import path: igive-hero.png"
    - path: "src/pages/nettside-for-bedrift/_sections/VisualProof.astro"
      provides: "Updated import path: igive-hero.png"
  key_links:
    - from: "src/assets/images/*.png"
      to: "src/config/projects.ts"
      via: "import iGiveImage from '@/assets/images/igive-hero.png'"
      pattern: "import.*igive-hero\\.png"
---

<objective>
Capture all four project screenshots per the visual content plan and rename the existing iGive image — with all three import sites updated atomically.

Purpose: Phase 22 imports these PNG files via Astro's &lt;Image&gt; component. If any file is missing at build time, Astro throws ENOENT and the build fails hard. All files must exist in src/assets/images/ before phase 22 creates any .astro import.
Output: Four PNG files committed to src/assets/images/, old salg.igive.no.png removed, three import statements updated, npm run build passing.
</objective>

<execution_context>
@/Users/iverostensen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iverostensen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/VISUAL-CONTENT-PLAN.md
@.planning/phases/20-innholdsforutsetninger/20-CONTEXT.md
@.planning/phases/20-innholdsforutsetninger/20-RESEARCH.md
</context>

<interfaces>
<!-- Three import sites that reference salg.igive.no.png — all must be updated atomically -->

From src/config/projects.ts line 2:
```typescript
import iGiveImage from '@/assets/images/salg.igive.no.png';
```
Change to:
```typescript
import iGiveImage from '@/assets/images/igive-hero.png';
```

From src/pages/_home/ProjectTeaser.astro line 3:
```typescript
import iGiveImage from '@/assets/images/salg.igive.no.png';
```
Change to:
```typescript
import iGiveImage from '@/assets/images/igive-hero.png';
```

From src/pages/nettside-for-bedrift/_sections/VisualProof.astro line 9:
```typescript
import iGiveImage from '@/assets/images/salg.igive.no.png';
```
Change to:
```typescript
import iGiveImage from '@/assets/images/igive-hero.png';
```

The variable name `iGiveImage` stays the same — only the import path changes.
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Capture screenshots for both projects</name>
  <files>src/assets/images/igive-hero.png, src/assets/images/igive-features.png, src/assets/images/blom-hero.png, src/assets/images/blom-features.png</files>
  <action>
    Human uses Chrome DevTools device emulation to capture four PNG screenshots at 1600px viewport width and saves them directly to src/assets/images/ with the exact filenames from the visual content plan.
  </action>
  <what-claude-prepared>
    VISUAL-CONTENT-PLAN.md has the exact crop guides for each screenshot. Required files: igive-hero.png, igive-features.png, blom-hero.png, blom-features.png. All must be 1600px wide (hero shots: 1600x900; feature crops: 1600px wide max, height can vary).
  </what-claude-prepared>
  <how-to-verify>
    Use Chrome DevTools to set a precise viewport:
    1. Open DevTools → Toggle device toolbar (Cmd+Shift+M)
    2. Set custom device size: Width=1600, Height=900
    3. Capture each screenshot per the crop guides in VISUAL-CONTENT-PLAN.md:
       - **igive-hero.png**: salg.igive.no — full viewport at top of page (1600x900)
       - **igive-features.png**: salg.igive.no — scroll to features/benefits section, capture that section
       - **blom-hero.png**: blomcompany.com (or blom-no.vercel.app if DNS fails) — full viewport at top (1600x900)
       - **blom-features.png**: blomcompany.com — scroll to product/collection section, capture
    4. Save all four files to src/assets/images/ using the exact filenames above
    5. Verify dimensions: `sips -g pixelWidth -g pixelHeight /Users/iverostensen/nettup/src/assets/images/igive-hero.png`

    Note: If you used blom-no.vercel.app instead of blomcompany.com, mention it so Claude can update the URL in the plan doc.
  </how-to-verify>
  <resume-signal>Type "screenshots saved" once all four PNG files are in src/assets/images/ with correct filenames. Optionally note if you used the fallback URL for Blom.</resume-signal>
  <verify>
    <automated>ls /Users/iverostensen/nettup/src/assets/images/igive-hero.png /Users/iverostensen/nettup/src/assets/images/igive-features.png /Users/iverostensen/nettup/src/assets/images/blom-hero.png /Users/iverostensen/nettup/src/assets/images/blom-features.png 2>&1 | grep -c "No such" | xargs test 0 -eq && echo "OK — all four files present" || echo "FAIL — missing files"</automated>
  </verify>
  <done>All four PNG files exist in src/assets/images/ with correct kebab-case filenames.</done>
</task>

<task type="auto">
  <name>Task 2: Rename salg.igive.no.png and update all three import sites atomically</name>
  <files>src/config/projects.ts, src/pages/_home/ProjectTeaser.astro, src/pages/nettside-for-bedrift/_sections/VisualProof.astro</files>
  <action>
    The user has placed igive-hero.png in src/assets/images/ (fresh recapture). The old file salg.igive.no.png must be removed and all three import sites updated in one atomic change.

    Steps:
    1. Delete src/assets/images/salg.igive.no.png (if it still exists — the user may have replaced it by saving igive-hero.png directly)
    2. In src/config/projects.ts: change `import iGiveImage from '@/assets/images/salg.igive.no.png'` to `import iGiveImage from '@/assets/images/igive-hero.png'`
    3. In src/pages/_home/ProjectTeaser.astro: same import path change
    4. In src/pages/nettside-for-bedrift/_sections/VisualProof.astro: same import path change
    5. Verify zero remaining references: `grep -r "salg\.igive\.no\.png" /Users/iverostensen/nettup/src/` must return nothing
    6. If user reported using blom-no.vercel.app instead of blomcompany.com, update the URL column for Blom Company in .planning/VISUAL-CONTENT-PLAN.md
    7. Update all four screenshot Status entries in VISUAL-CONTENT-PLAN.md from `[ ]` to `[x]`

    Do NOT change the variable name `iGiveImage` — only the import path string.
  </action>
  <verify>
    <automated>grep -r "salg\.igive\.no\.png" /Users/iverostensen/nettup/src/ | wc -l | xargs test 0 -eq && echo "OK — no old references" || echo "FAIL — old references remain"</automated>
  </verify>
  <done>salg.igive.no.png is gone, all three import sites reference igive-hero.png, VISUAL-CONTENT-PLAN.md shows [x] for all four screenshots.</done>
</task>

<task type="auto">
  <name>Task 3: Validate build passes with all committed images</name>
  <files></files>
  <action>
    Run the production build to confirm no ENOENT errors:

    `cd /Users/iverostensen/nettup && npm run build`

    Expected: build completes with "Done in X.Xs" and zero errors.

    If build fails with ENOENT for any PNG:
    - Check which filename is missing in src/assets/images/
    - Verify the file was saved there (not in Downloads or Desktop)
    - Ensure the import in the relevant .ts or .astro file matches the exact filename on disk

    Do NOT proceed to phase 21 if this build fails.
  </action>
  <verify>
    <automated>cd /Users/iverostensen/nettup && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>npm run build exits 0 with no ENOENT errors. All four PNG files confirmed present in src/assets/images/. Phase 20 is complete.</done>
</task>

</tasks>

<verification>
- ls src/assets/images/*.png includes igive-hero.png, igive-features.png, blom-hero.png, blom-features.png
- grep -r "salg.igive.no.png" src/ returns nothing
- npm run build passes clean
- VISUAL-CONTENT-PLAN.md shows [x] for all four screenshot entries
</verification>

<success_criteria>
INNHOLD-03 satisfied: All four PNG screenshots committed to src/assets/images/ per the visual content plan filenames. salg.igive.no.png removed, three import sites updated, npm run build passes.
</success_criteria>

<output>
After completion, create `.planning/phases/20-innholdsforutsetninger/20-03-SUMMARY.md`
</output>
